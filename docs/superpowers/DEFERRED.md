# Deferred backlog — 0.3.0 slice (from the v0.1.2 review)
Source: v0.1.2 review waves (generated 2026-08-22). Living work list: each
item gets its disposition recorded here when implemented (see spec
`2026-08-22-v0.2.0-design.md` §5; beads epic `yolo-8vl`, 0.3.0 slice `yolo-k98`).
## Summary
| skill | findings | fixed | false | deferred | wontfix |
|---|---|---|---|---|---|
| golang-concurrency (wave 1) | 23 | 12 | 0 | 11 | 0 |
| golang-safety (wave 2) | 22 | 18 | 0 | 4 | 0 |
| golang-error-handling (wave 3) | 24 | 19 | 0 | 5 | 0 |
| golang-troubleshooting (wave 4) | 10 | 8 | 0 | 2 | 0 |
| golang-security (wave 5) | 8 | 6 | 0 | 2 | 0 |
| golang-database (wave 6) | 8 | 6 | 0 | 2 | 0 |
| golang-design-patterns (wave 7) | 17 | 12 | 0 | 5 | 0 |
| golang-refactoring (wave 8) | 16 | 0 | 0 | 16 | 0 |
| golang-cli (wave 9) | 11 | 4 | 0 | 7 | 0 |
| golang-testing (wave 10) | 34 | 32 | 0 | 2 | 0 |
| golang-data-structures (wave 11) | 13 | 12 | 0 | 1 | 0 |
| golang-performance (wave 12) | 7 | 7 | 0 | 0 | 0 |
| golang-benchmark (wave 13) | 8 | 8 | 0 | 2 | 0 |
| golang-naming (wave 14) | 50 | 42 | 0 | 8 | 0 |
| golang-code-style (wave 15) | 25 | 24 | 0 | 1 | 0 |

*Wave 13 (benchmark) is assessment-only: its 8 "findings" are delivered benchmarks (Stats P3:8); its 2 deferred items are benchmark candidates, not findings — the row intentionally does not sum (8 delivered + 2 candidates). The 14 defect waves sum exactly: 268 findings = 202 fixed + 66 deferred (22 of the 88 deferred findings closed by the 0.2.0 work listed above; the remainder stay `0.3.0 slice`, tracked by `yolo-k98`).*

Notes: wave 8 is the assessment-only 0.2.0 refactor backlog (16 entries, copied verbatim below); wave 13's 8 items are the shipped hermetic benchmarks and its 2 deferred items are benchmark candidates (no 0.1.2 evidence to pin baselines against). Untagged-status findings carry a non-`none` contract-risk tag (behavior/wire/render/pin) and are auto-deferred per fix policy B.
## Per-skill details
### golang-concurrency (wave 1)
- [concurrency-1] P1 internal/llm/openai.go:32 (also anthropic.go:38) — non-2xx path closes resp.Body before oaUpstreamError drains it, so all 4xx/5xx surface as generic status text and the API error envelope is never decoded — reason: DEFERRED contract (tag: behavior) — **fixed in 0.2.0**: `fix: drain LLM API error body before close (64 KiB cap, decoded message)` (④)
- [concurrency-1] P0 internal/permission/service.go:96 — data race: decisionFor reads s.dataDir unlocked while SetDataDir writes it under s.mu; reachable with 2+ concurrent sessions, -race-detectable — reason: DEFERRED contract (tag: behavior) — **fixed in 0.2.0 (verified stale)**: `SetDataDir` no longer exists (removed in `39a196e`); the `-race` regression guard was added anyway — `test: concurrent-sessions race regression guard for the permission service` (①)
- [concurrency-2] P1 internal/permission/service.go:179-181, 266-289 — double-settle race: Reply + ctx-cancel in the same select both resolve, so one requestID gets two permission.replied events and flips the storage row — reason: DEFERRED contract (tag: wire) — **fixed in 0.2.0**: `fix: settle a parked permission request exactly once` (②)
- [concurrency-3] P2 internal/permission/service.go:276-278 — a real db.ReplyPermission error removes the pending entry without delivering, so the parked Ask blocks until ctx.Done (engine wedged until abort) — reason: DEFERRED contract (tag: behavior) — **stale — resolved in 0.2.0**: `resolve()` persists best-effort and delivers regardless of the DB result (`permission/service.go:256-286`); absorbed by 0.2.0 ②
- [concurrency-2] P1 internal/server/handlers_misc.go:180-197 — unserialized read-modify-write of yolo.jsonc (PATCH /config and /global/config): concurrent callers, last write wins, the other merge is silently lost — reason: DEFERRED contract (tag: wire) — **fixed in 0.2.0**: `fix: serialize yolo.jsonc read-modify-write (concurrent PATCH lost update)` (③)
- [concurrency-1] P2 internal/session/engine.go:315-328 — runTurn has no panic recovery: a tool/driver/DB panic escapes the turn goroutine, onDone(nil) reports false success, and the process crashes — reason: DEFERRED contract (tag: behavior) — **fixed in 0.3.0**: `fix: recover panics in the engine turn goroutine`
- [concurrency-2] P2 internal/session/engine.go:201-211 — Close (and Shutdown overflow path) removes/closes the session's *tool.Shell without waiting for a busy turn → use-after-close on an in-flight bash — reason: DEFERRED contract (tag: behavior) — **half stale (Shutdown half) — resolved in 0.2.0**: Shutdown drains `busy` up to 5 s before closing shells (`engine.go:264-299`); the Close half stays open (→ Plan 1 task D, spec §2.2/§2.4a) — **fixed in 0.3.0 (Close half)**: `fix: delete-while-busy aborts the turn and defers the shell close`
- [concurrency-4] P3 internal/session/engine.go:1053-1054 — title goroutine spawned from a 30 s background ctx: not signalled by Abort/Shutdown, lingers up to 30 s after session teardown — reason: DEFERRED contract (tag: behavior) — **fixed in 0.3.0**: `fix: track the title goroutine ctx (abort + shutdown)`
- [concurrency-5] P3 internal/session/engine.go:151-159 — Send early-fail path publishes a session.status idle with no preceding busy (lone SSE transition no client observed a start to) — reason: DEFERRED contract (tag: wire) — **fixed in 0.3.0**: `fix: send early-fail publishes no lone idle status`
- [concurrency-6] P3 internal/session/engine.go:190-197 — Abort TOCTOU: cancel read under lock, invoked after unlock; a new turn starting in the window keeps running while Abort reports success — reason: DEFERRED contract (tag: behavior) — **fixed in 0.3.0**: `fix: abort invokes the turn cancel under the lock`
- [concurrency-1] P2 main.go:278 — logger double-Close on every draining exit path (drain + closeDB); a non-idempotent Close panics at exit and replaces the exit code with a stack trace — reason: auto-defer (contract-risk: behavior; wave-1 fix subagent never dispatched) — **stale — resolved in 0.2.0**: `Logger.Close` is idempotent and nil-safe (`internal/log/log.go:257-271`); 0.2.0 slog rework
- [concurrency-2] P2 app.go:290 (also :224, :241) — createSessionCmd and both hydrateCmd branches run on context.Background with no timeout; a hung REST call freezes the TUI forever in "loading…" — reason: auto-defer (contract-risk: behavior) — **stale — resolved in 0.2.0**: every TUI REST cmd carries a 5 s `context.WithTimeout` (`app.go:258,276,329,480,532,681,726,733,778`); 0.2.0
- [concurrency-3] P3 client.go:45 — SSE backoff `time.Second << uint(n)` overflows at n ≥ 34 and returns a negative duration → tight reconnect loop after ~15 min of continuous failure — reason: auto-defer (contract-risk: behavior) — **stale — resolved in 0.2.0**: the shift is clamped `min(n,5)` with a 30 s cap (`tui/client/client.go:41-52`); 0.2.0
- [concurrency-4] P3 app.go:134 — "● live" footer Conn only flips when the Events channel closes (process exit); transient SSE drops render "live" throughout the outage — reason: auto-defer (contract-risk: render)
- [concurrency-5] P3 main.go:234 — pre-Run SIGINT is buffered into a never-read stop channel (silently lost, lands in TUI); second SIGTERM during serve drain dropped (no force-kill path) — reason: auto-defer (contract-risk: behavior) — **half stale (pre-Run SIGINT half) — resolved in 0.2.0**: the stop channel is now read by a log-only goroutine (`cmd/yolo/main.go:296-301`); the second-SIGTERM half stays open (→ Plan 1 task X, spec §2.2)
### golang-safety (wave 2)
- [safety-2] P1 internal/storage/dao.go:350 — PartToProtocol never restores Time.Start: after a restart every persisted part reloads with start:0 (upstream keeps real starts) — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0**: `fix: restore part Time.Start on decode from TimeCreated` (⑤)
- [safety-3] P2 internal/storage/dao.go:308 — ProtocolToPart swallows the tool-state json.Marshal error: NaN/Inf input persists StateJSON="" and every later read 500s the whole message — reason: auto-defer (contract-risk: behavior) — **fixed in 0.3.0**: `fix: return ProtocolToPart marshal errors instead of empty strings`
- [safety-4] P2 internal/storage/dao.go:274 — listPartsBy orders by time_created ASC alone; same-millisecond parts load in undefined (possibly non-turn) order — reason: auto-defer (contract-risk: behavior) — **fixed in 0.3.0**: `fix: deterministic storage ordering via rowid tiebreak`
- [safety-1] P2 internal/server/handlers_misc.go:191 — unsynchronized ReadFile→merge→WriteFile of yolo.jsonc in writeProjectLayer/handleGlobalConfigPatch: lost update under concurrent PATCH — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0** (absorbed by ③): `fix: serialize yolo.jsonc read-modify-write (concurrent PATCH lost update)`
- [safety-1] P1 internal/llm/openai.go:31-34 — read-after-close on resp.Body: every API 400 degrades to generic status text and the isOverflowError graceful-overflow path is dead for provider 400s — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0** (absorbed by ④): `fix: drain LLM API error body before close (64 KiB cap, decoded message)`
- [safety-3] P1 internal/tool/bash.go:83-90 — model-supplied timeout:0 disables the kill timer (`timeoutMS <= 0`): a hung command runs unbounded until manual abort — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0** (absorbed by ⑥): `fix: reject bash timeout <= 0 and clamp to 2^31-1 ms`
- [safety-4] P2 internal/tool/shell.go:130-136 — EOF-without-marker branch returns a successful Output with Meta exit:-1: a process that died mid-command is told the model it succeeded — reason: auto-defer (contract-risk: behavior)
- [safety-2] P2 internal/tui/client/event.go:58 — SSE line scanner max token is 1 MiB: a single >1 MiB data: line (tool output ~700 KB+ escaped) makes Scan() fail and the event is silently dropped — reason: auto-defer (contract-risk: behavior)
### golang-error-handling (wave 3)
- [error-2] P2 internal/storage/dao.go:308 — ProtocolToPart discards json.Marshal errors (tool state + text map at :318): state lost at write time, later PartToProtocol fails context-less — reason: DEFERRED contract (behavior — persisted part bytes would change on the marshal-failure path) — **fixed in 0.3.0** (merged with the [safety-3] row — same marshal sites): `fix: return ProtocolToPart marshal errors instead of empty strings`
- [error-3] P3 internal/storage/dao.go:149 (also :186, :428) — RowsAffected error discarded and mapped to ErrNotFound: a driver-level failure is masked as not-found — reason: DEFERRED contract (behavior — error identity changes in the RowsAffected-failure corner) — **fixed in 0.3.0**: `fix: return RowsAffected errors instead of ErrNotFound`
- [error-3] P2 internal/tool/grep.go:148-151 — os.Stat failure on the searched root is swallowed → nonexistent search dir reported as "No files found" (misleading; upstream errors) — reason: DEFERRED contract (behavior — a grep stat error on a nonexistent root changes tool output)
- [error-5] P3 cmd/yolo/main.go:255-258 — tea.Run runtime error mapped only to an exit code: TUI start failure (no tty, I/O error) exits 1 with zero diagnostic — reason: DEFERRED contract (behavior — visible stderr on a tea.Run failure)
- [error-6] P3 cmd/yolo/main.go:362 — `yolo auth add` discards the ReadString error: on EOF/broken pipe the trimmed empty line is persisted as the key, blanking the credential — reason: DEFERRED contract (behavior — auth add read-failure handling changes CLI exit/behavior)
- [error-7] P3 internal/tool/bash.go:83-90 — error message says "timeout must be a positive integer" but argInt accepts 0 and `<= 0` disables the 120 s guard — reason: DEFERRED contract (behavior — rejecting timeout 0 changes accepted inputs) — **fixed in 0.2.0** (absorbed by ⑥): `fix: reject bash timeout <= 0 and clamp to 2^31-1 ms`
### golang-troubleshooting (wave 4)
- [troubleshoot-2] P2 internal/tool/bash.go:83-90 — `timeout: 0` accepted (argInt admits 0) → Shell.Exec skips the timer: the pinned 120 s per-command guard silently disabled (also an upstream deviation) — reason: DEFERRED contract (behavior — rejecting timeout 0 changes accepted inputs; 0→kill-at-100ms would itself be an upstream deviation) — **fixed in 0.2.0** (absorbed by ⑥): `fix: reject bash timeout <= 0 and clamp to 2^31-1 ms`
- [troubleshoot-5] P3 internal/server/handlers_session.go:194-211 (+ engine.go:201-211, 305-314) — deleting a session with an ACTIVE turn does not cancel the turn: events keep publishing for the deleted session and a post-Close tool call re-spawns a leaked shell process — reason: DEFERRED contract (behavior — delete-while-busy abort semantics change; needs-upstream-check unresolved) — **fixed in 0.3.0**: `fix: delete-while-busy aborts the turn and defers the shell close`
- [troubleshoot-1] P1 internal/session/engine.go:853 — overflowRe `(?i)(context|tokens?|too long|exceeds)` over-matches: 401/403/413 errors reclassified as context overflow, turn ends idle (turnErr==nil) with a synthetic note, real auth/quota failure never surfaced — reason: DEFERRED contract (behavior — narrow overflow pattern list changes which provider errors are surfaced, retried, or end with a synthetic note) — **fixed in 0.2.0**: `fix: narrow overflow classifier to the upstream curated list` (⑦)
- [troubleshoot-2] P1 internal/session/engine.go:54 — Deps.Log "nil = no-op" not honored: every call site dereferences lg; an Engine built without Log panics the process on the first persistence/marshal error — reason: DEFERRED contract (behavior — defaulting a no-op logger in New changes the documented Deps.Log nil contract) — **fixed in 0.2.0** (logging stream): `feat: slog-based leveled file logger with run id, level env, and new log points` (engine `New` now defaults a nil `Deps.Log` to `log.Noop()`)
- [troubleshoot-3] P2 internal/session/engine.go:758 — text/reasoning part re-finalized and its ID reused across tool parts: tool rounds emit ≥2 final part.updated frames per text part; upstream mints a fresh part id per text block — reason: DEFERRED contract (wire — tool-round SSE part-id reuse/re-finalization changes the part-frame sequence and transcript structure) — **fixed in 0.3.0**: `fix: mint a fresh part id per text block after a tool call`
- [troubleshoot-4] P2 internal/session/engine.go:350 — per-turn SetDataDir/SetConfigRules on the single shared permission.Service: concurrent turns of different projects can interleave Set/Ask (cross-project rule bleed) — reason: DEFERRED contract (behavior — changing the shared permission.Service set-state to parameters is a cross-package API/flow change) — **fixed in 0.2.0** (absorbed by ⑧): `fix: pass permission config rules per request (drop shared state)`
### golang-security (wave 5)
- [security-1] P2 internal/server/server.go:163 — http.Server has no ReadHeaderTimeout/ReadTimeout: exposed `yolo serve --addr` binds are open to Slowloris-style header exhaustion — reason: DEFERRED contract (behavior — timeouts make slow/abusive clients disconnect observably; SSE forces Idle/WriteTimeout to stay unset) — **stale — resolved in 0.2.0**: `ReadHeaderTimeout: 10 s` is set (`server/server.go:171`); 0.2.0
- [security-1] P1 internal/tool/glob.go:115-147 — walk collects ALL matching paths before the 100-result cap, no early stop: `glob {"/"}` walks the whole filesystem, unbounded memory (GB-scale) reachable by an always-approved model call — reason: DEFERRED contract (behavior — stopping the walk early or dropping the full-tree sort changes which paths fill the 100-result output) — **fixed in 0.2.0**: `fix: bound the glob walk at the result cap (early stop)` (⑫; output order walk-order, deviation 88)
- [security-2] P2 internal/tool/bash.go:83-90 (+ shell.go:84,117-121) — timeout 0 removes the per-command bound and huge n overflows time.Duration into an arbitrary wrapped kill instant; upstream declares PositiveInt — reason: DEFERRED contract (behavior — rejecting/clamping timeout ≤ 0 and huge values changes observable execution) — **fixed in 0.2.0** (absorbed by ⑥): `fix: reject bash timeout <= 0 and clamp to 2^31-1 ms`
- [security-3] P2 internal/tool/write.go:119-143 (also edit.go:155,157) — naive full-DP LCS on every write/edit: a one-line edit of a ~60 k-line file is ~3.6e9 DP cells, tens of seconds of blocked engine CPU — reason: DEFERRED contract (behavior — swapping DP-LCS for Myers/region-restricted diff can change the added/removed counts in tool output)
- [security-5] P3 internal/log/log.go:62 (also :41) — write() emits text unvalidated: embedded newlines/control chars (paths, error strings, panic stacks) forge additional log lines (CWE-117) — reason: DEFERRED contract (behavior — sanitizing newlines/control runes changes the bytes written to the log file) — **fixed in 0.2.0** (logging stream): `feat: slog-based leveled file logger with run id, level env, and new log points` (pinnedHandler quotes/escapes ` \t\n\r"\\` in k=v values; unquoted msg escapes control chars)
### golang-database (wave 6)
- [database-3] P2 internal/storage/dao.go:68 (systemic) — zero context propagation: no ctx parameters and no *Context variants anywhere in the storage DAOs; cancelled requests cannot cancel in-flight DB ops — reason: DEFERRED 0.2.0 (API signature churn across storage/permission/session/server; finding itself recommends a deliberate 0.2.x change) — **fixed in 0.3.0**: `refactor: thread context through the storage DAOs (in-place signature change)`
- [database-6] P3 internal/storage/dao.go:275 (also :201, :480) — ORDER BY time_created ASC has no deterministic tiebreaker: same-millisecond rows return in query-plan order, part/message order on reload can drift — reason: auto-defer (contract-risk: render; needs-upstream-check unresolved — upstream clone missing on this host) — **fixed in 0.3.0** (merged with the [safety-4] ordering row; yolo's lists are cursor-less ASC — the rowid tiebreak is the port-faithful form of the upstream id tiebreak): `fix: deterministic storage ordering via rowid tiebreak`
### golang-design-patterns (wave 7)
- [design-1] P1 internal/session/engine.go:350 (+ permission/service.go:54,73,104) — shared Service.cfgRules global: every ask re-evaluated against the most recent turn's config rules; concurrent sessions in different project dirs contaminate each other's verdicts — reason: auto-defer (behavior; 0.2.0 backlog) — **fixed in 0.2.0**: `fix: pass permission config rules per request (drop shared state)` (⑧)
- [design-3] P2 internal/session/engine.go:216,1073,1086 — Shutdown drain is incomplete: generateTitle goroutines (30 s background ctx, untracked) can write the DB and publish session.updated after Shutdown returns and the store closes — reason: auto-defer (behavior; 0.2.0 backlog) — **fixed in 0.3.0** (folded into the title-goroutine task): `fix: track the title goroutine ctx (abort + shutdown)`
- [design-5] P2 internal/permission/service.go:199,281 — Reply is check-then-act across two lock acquisitions: two concurrent replies to one requestID both pass, the loser emits an orphan permission.replied + DB write — reason: auto-defer (behavior; 0.2.0 backlog) — **stale — resolved in 0.2.0**: `resolve()` claims atomically (lock → re-lookup → delete, loser returns early; `service.go:261-271`); absorbed by 0.2.0 ②
- [design-7] P3 internal/session/prompt.go:124 — gitCache is an unbounded, non-expiring package-global map: a dir that becomes a git repo mid-process stays "no" forever; env-block text baked into every system prompt — reason: auto-defer (behavior — system-prompt content is observable model input; 0.2.0 backlog) — **fixed in 0.3.0**: `fix: bound the git repo cache with a TTL`
- [design-1] P1 internal/server/handlers_misc.go:96-112 — authState is a second, incomplete key-resolution implementation missing the options.apiKey branch: a provider configured with ONLY options.apiKey reports "missing" while the runtime uses the key — reason: auto-defer (wire; 0.2.0 backlog) — **fixed in 0.2.0**: `fix: authState honors options.apiKey (parity with runtime resolution)` (⑨)
- [design-4] P2 internal/server/handlers_misc.go:129-131 — handleProviderAuth hardcodes s.WorkDir (ignores x-yolo-directory) while GET /provider is request-scoped: the two endpoints of one resource family report against different directories — reason: auto-defer (wire; 0.2.0 backlog)
- [design-5] P2 internal/tui/app.go:133-135 — connLostMsg clears store.Conn but never re-hydrates: every event missed in the SSE outage window is lost from the store forever — reason: auto-defer (behavior; 0.2.0 backlog) — **stale — resolved in v0.1.3/0.2.0**: the v0.1.3 resync path re-hydrates the route over REST (`resyncMsg`, `app.go:143-148`); `connLostMsg` now fires only on channel close (teardown)
### golang-cli (wave 9)
- [cli-1] P2 cmd/yolo/main.go:276 — tea.Run error only mapped to an exit code: a TUI that fails to start (no TTY, I/O error) exits 1 with zero diagnostic, unlike every other error path — reason: auto-defer (contract-risk: behavior)
- [cli-2] P2 cmd/yolo/main.go:252 — stop signal channel never read: SIGINT (InterruptMsg, unhandled by the model) is permanently swallowed; only SIGTERM (QuitMsg) ends the TUI — reason: auto-defer (contract-risk: behavior)
- [cli-3] P2 cmd/yolo/main.go:380 — auth add ignores ReadString's error and never validates the key: `yolo auth add kido </dev/null` silently persists an empty credential — reason: auto-defer (contract-risk: behavior)
- [cli-4] P3 cmd/yolo/main.go:49 — -v/--version unsupported: dispatcher falls through to tuiMode and fails flag parsing with exit 2 — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0** (version stream): `feat: version wiring (ldflags + VCS stamp) and justfile entry point`
- [cli-5] P3 cmd/yolo/main.go:58 — version "0.0.0-dev" hardcoded; no ldflags injection point exists for 0.2.0 versioning — reason: auto-defer (contract-risk: behavior) — **fixed in 0.2.0** (version stream): `feat: version wiring (ldflags + VCS stamp) and justfile entry point`
- [cli-6] P3 cmd/yolo/main.go:51 — `yolo help`/-h/--help prints usage to stderr: `yolo help | less` and capture scripts see an empty stream — reason: auto-defer (contract-risk: behavior)
- [cli-7] P3 cmd/yolo/main.go:304 — unexpected positional args silently ignored by serve/auth (`yolo serve junk` serves fine; `auth add kido k1 k2` drops extras) — reason: auto-defer (contract-risk: behavior) — **half stale (tuiCmd half) — resolved in 0.2.0**: tuiCmd rejects `NArg > 1` (`main.go:259-262`); the serve/auth halves stay open (→ Plan 1 task AA, spec §2.2)
- [cli-9] P3 scripts/e2e-live.sh:57 — only `trap cleanup EXIT` set: bash skips the EXIT trap on untrapped SIGINT/SIGTERM, so `kill <script-pid>` leaves the backgrounded `yolo serve` running and billing — reason: auto-defer (contract-risk: behavior)
- [cli-10] P3 scripts/e2e-live.sh:153 — step-3 timeout branch never shows the server log tail: a server that died 2 s into the turn is indistinguishable from a slow turn — reason: auto-defer (contract-risk: behavior)
### golang-testing (wave 10)
- [testing-1] P2 internal/tui/permission_test.go:128-158 — TestPermissionKeyGate asserts only opaque-cmd counts: key→reply wiring (1→once, 2→always, 3→reject) unpinned; a '1'→always mixup passes the entire suite — reason: DEFERRED (auto-deferred by wave rule — fix pins persisted-rule observable state: contract-risk: behavior)
- [testing-2] P2 internal/tui/model_test.go:199-220 — dialog subchoice 'b' (set default → config PATCH) never executed end-to-end: a 'b' wired to the session-patch path or wrong field passes everything — reason: DEFERRED (auto-deferred by wave rule — fix pins the config-patch target: contract-risk: behavior)
### golang-data-structures (wave 11)
- [datastruct-9] P3 internal/tui/app.go:418 — multi-line prompt draft grows with unamortized string concat (quadratic in total draft bytes across soft-enters) — reason: DEFERRED 0.2.0 (user-paced, low-impact path; negligible at realistic draft sizes; conversion would touch prompt_test.go draft assertions for no visible gain)
### golang-performance (wave 12)
 - [performance-1] P2 internal/session/engine.go:686 (saveDelta/startPart/finalizePart + dao.go UpsertPart/ProtocolToPart) — every streamed delta re-encodes the ENTIRE accumulated part text and re-writes the whole state_json row: O(n²) bytes per part on the turn goroutine's critical path — reason: deferred to 0.2.0 (contract-risk: behavior — fix: persist full text at part finalization, keep per-delta events for the wire; benchmarks attached in wave 13) — **fixed in 0.2.0**: `perf: persist part text only at finalization, not per delta` (⑩)
 - [performance-7] P2 internal/session/engine.go:449-556 — messagesFor re-derives the whole request from the DB every round (1+M queries, per-part JSON decode, per-tool re-marshal): total turn cost grows O(R²·M) over up to 50 rounds — reason: deferred to 0.2.0 (contract-risk: behavior — fix: per-turn history snapshot with incremental append; benchmarks attached in wave 13) — **fixed in 0.2.0**: `perf: snapshot turn history in memory instead of re-querying per round` (⑪)
### golang-benchmark (wave 13)
- [candidate-9] (*Store).Apply (internal/tui/store/store.go:29) — no benchmark added: wave 12 verified the fold (appendDelta zero-copy via builder aliasing) and there is no hot-path evidence to pin a baseline against — reason: deferred (no 0.1.2 evidence; revisit 0.2.0 if the render path changes)
- [candidate-10] Truncate (internal/tool/truncate.go:9) — no benchmark added: no performance-wave evidence bounding it; single-pass tail cut, semantics pinned by deviation 11 — reason: deferred (not on a measured hot path)
### golang-naming (wave 14)
- [naming-9] P3 internal/protocol/provider.go:5-7,20-22 (also agent.go:10, part.go:30-31, event.go:103) — wire-DTO bool fields lack is/has predicate form (KeyRequired, ToolCall, Reasoning, Attachment, Hidden, Synthetic, Ignored, Auto); any rename changes JSON tags — reason: DEFERRED contract (wire-tagged; JSON-tag change)
- [naming-10] P3 internal/protocol/session.go:28-31 — StatusIdle/StatusBusy/StatusRetry not prefixed with owning type SessionStatus: ambiguous beside Todo.Status/ToolState.Status — reason: DEFERRED contract (behavior-tagged; 12 files > 5)
- [naming-11] P3 internal/server/deps.go:6 — import alias fakellm resolves no name collision (package is `fake`); alias rename spans 10 files across 3 packages — reason: DEFERRED contract (behavior-tagged; 10 files > 5 across 3 packages)
- [naming-1] P3 internal/tui/client/client.go:29 — package/type stutter client.Client at every call site — reason: DEFERRED contract (behavior-tagged; client.Client stutter rename spans the whole package)
- [naming-2] P3 internal/tui/store/store.go:15 — package/type stutter store.Store (call sites `a.store store.Store`) — reason: DEFERRED contract (behavior-tagged; store.Store stutter rename spans the whole package)
- [naming-3] P3 internal/tui/client/client.go:22-24 — sentinel error strings lack the "client: " package prefix; origin lost when wrapped and the text is what the user sees in the status line — reason: DEFERRED contract (render-tagged; changes the status-line text the user sees)
- [naming-5] P3 internal/tui/app.go:526 — enum zero value is a real dialog: zero-initialized dialog{} silently becomes dlgQuit — reason: DEFERRED 0.2.0 (additive sentinel constant is not a rename and renumbers the other constants)
- [naming-13] P3 internal/tui/model_test.go:31 — tuiProviderFixture/tuiAgentFixture anti-stutter rename blocked: agentFixture() already exists at agent_test.go:20 — reason: DEFERRED (collision — needs a cascading rename beyond this finding's scope)
### golang-code-style (wave 15)
- [style-007] P3 internal/tool/glob.go:40 — 294-col line: the "path" parameter description inside the glob tool's JSON schema literal (faithful upstream text); a value-preserving `+` break is possible but the emitted schema string must stay byte-identical — reason: auto-defer (pin tag)
### refactor backlog (wave 8)
- [refactor-1] P2 internal/session/engine.go (1192) — too-large — single file bundles four concerns: turn lifecycle (New/Deps/Send/Status/Abort/Close/Shutdown/idleAndRelease/publish), history→LLM mapping, tool execution, title generation — split into: `engine.go` (turn lifecycle only: construction, busy-map, Send/Status/Abort/Close/Shutdown, runTurn/deps helpers), `history.go` (persisted history → llm.Message mapping: messagesFor/joinTextParts/appendReminders/isSyntheticPart), `tool_exec.go` (one model tool call through the permission gates: executeTool/saveToolPart/callKeyHash/withinDir), `title.go` (one-shot session title generation: maybeScheduleTitle/generateTitle), `round.go` (one streamed model round: runRound/finishRound/saveSynthetic/overflowRe/isOverflowError/overflowNote) — each unit owns exactly one responsibility; all moves are same-package, no exported API change — effort: M — contract-risk: none (pure file moves; gate green per move)
- [refactor-2] P2 internal/session/engine.go:582 — complex-function — `runRound` is 232 lines with 12 parameters and three closures over a `textState` struct (startPart/saveDelta/finalizePart); the pre-stream retry loop, the delta-accumulation state machine and the tool-step budget are three interleaved concerns in one function — extract: `partState` type replacing textSt/reasonSt, with `Start(kind, delta)`/`Delta(kind, delta)`/`Finalize(kind)` methods owning id/start/buf + upsert + publish (one responsibility: accumulate and persist one streamed text/reasoning part); `streamWithRetry(drv, req, roundCtx)` helper owning the pre-stream transient-retry loop (one responsibility: obtain a stream or a terminal round error with the retry session.status publishes); a `roundInput` struct replacing the 12-arg signature (sessionID/agent/row/cfg/cfgRules/info/model/req/doomHist/toolCalls) — end state: runRound is the ~60-line round driver (loop over stream.Next, dispatch kinds, exit paths) — effort: M — contract-risk: behavior (pins at play: exactly one final part.updated, retry lifecycle LOCKED comment at :576-581, tool-step budget drop; engine_test.go + engine_perm_test.go must stay green unmodified)
- [refactor-3] P2 internal/session/engine.go:908 — complex-function — `executeTool` is 156 lines with 9 parameters, interleaving arg decode, tool lookup, ruleset load, hidden-guard, 4 permission gates (doom/hidden/external-dir/core) and tool execution; the per-call context is passed positionally through every gate — extract: a `toolCall` struct bundling (ctx, sessionID, agent, row, cfg, cfgRules, asstID, doomHist, part) — one responsibility: carry the state of one in-flight tool call; gate helpers `checkDoom`, `gateExternal`, `coreAsk` (one responsibility each: evaluate+park+finalize one permission gate, shared `gateFail` finalizer); end state: executeTool is a linear state machine ≤50 lines reading `tc` — effort: M — contract-risk: behavior (pins: doom ask fires BEFORE part goes running, denial/abort finalization wording — deviations 21/22; engine_perm_test.go must stay green unmodified)
- [refactor-4] P2 internal/llm/openai.go:167, internal/llm/anthropic.go:161 — duplicated — `oaReadSSE` (124 lines) and `anReadSSE` (108) are the same byte-level SSE pump (defer close(ch)+body.Close, ctx-cancel `send` closure, bufio ReadString loop, `data:` line buffering, blank-line frame flush, finished-flag + final `finish()`, EOF→readErr) differing only in the per-frame `process(payload)` handler and the finish payload — extract to `internal/llm/sse.go`: `sseLoop(ctx, body, process func(payload string), finish func())` (one responsibility: drain an SSE body into frames, respecting ctx and a finished flag, and emit the terminal finish) — drivers keep only their wire types, `process` handlers and finish logic; both readers shrink to ~55 lines and the pump's edge cases (ctx-cancel mid-frame, EOF with finished already set, `[DONE]`-indep finish) are implemented and tested once — effort: M — contract-risk: behavior (hot path #6; error/finish frame ordering is pinned by openai_test/anthropic_test — must stay green unmodified)
- [refactor-5] P2 internal/tui/app.go (831) — too-large — one file owns the root model plus 8 message-type/cmd pairs, the dialog stack, the key dispatcher and frame composition — split into: `hydrate.go` (initial + resume REST hydration: hydratedMsg/hydrateCmd/applyHydrate/sessionCreatedMsg/createSessionCmd/applySessionCreated/putSessionFirst/openSession — one responsibility: rehydrate the current route over REST), `dialog.go` (dialog kind/stack/view/key + catalog fetch + patch apply — one responsibility: the dialog stack and the model/agent pickers it hosts), `keys.go` (handleKey/handleMenuKey/handlePromptKey/inputUpdate/promptEnter — one responsibility: the key precedence ladder permission>dialog>openers>menu>route>prompt), `commands.go` (sendMsg/sendMessageCmd/applySend/runCommand/commandCmd/commandExecMsg/applyCommandExec/abortedMsg/abortCmd/quitCmd — one responsibility: user-initiated session actions), `view.go` (View/view/overlayLines/viewSession — one responsibility: compose the on-screen frame); app.go keeps App/NewApp/Init/Update/eventPump/emit; while there, delete the duplicated doc comment at :101-105 (Update documented twice by two consecutive comments) — effort: M — contract-risk: none (same-package moves; teatest suites + imports_test.go green = verified)
- [refactor-6] P3 internal/session/engine.go:437 — complex-function — `messagesFor` (108 lines, hot path #8) mixes DB read (N+1 ListParts per message), part decode+synthetic filter, and the LOCKED history→llm.Message mapping in one function, so the mapping logic is only testable through the engine — extract: `mapHistory(hist []protocol.MessageWithParts, agent string, sys []string) []llm.Message` (one responsibility: pure LOCKED mapping — system entries, user/assistant/tool roles, reminder attachment, empty-assistant skip (history mirrors upstream 1:1 — no re-append, deviation 77)) taking already-loaded history; messagesFor becomes a thin loader (system prompt, instruction files, DB read, decode, filter, call mapHistory) — effort: S — contract-risk: behavior (the LOCKED mapping doc at :423-436 is the contract; engine_test.go TestHistoryReplayIncludesToolResults + contract_test.go replays must stay green unmodified)
- [refactor-7] P3 internal/session/engine_test.go (684) — too-large — ~380 lines of shared test harness (harness struct, newHarness, replyWatcher, slowDriver, build, cfgLoader, startSession, event helpers, waitIdle/waitPart, request filters) are mixed with the 4 engine tests below them — split into `harness_test.go` (all of the above — one responsibility: engine test harness + event/DB poll helpers) leaving `engine_test.go` with the 4 tests; in the same move extract the assertion blocks of TestSingleTextTurnEndToEnd (135 lines: :445-478 persisted-state asserts, :502-542 event-order asserts) into `assertTurnState`/`assertTurnEvents` helpers — effort: S — contract-risk: none (test-only; gate green = verified)
- [refactor-8] P3 internal/server/contract_test.go (590) — too-large — four unrelated suites share one file (golden framework, SSE ordering, env-gated fake e2e, scope matrix) — split into: `golden_test.go` (normalizer/golden/TestGoldenResponses + shared mkSession/waitIdle helpers — one responsibility: REST response golden mirror), `sse_ordering_test.go` (sse* frame helpers + TestSSEOrdering, 97-line body — extract the index-collection loop :350-372 into a `frameIndex(...)` helper — one responsibility: faithful SSE frame-order pins), `fake_env_e2e_test.go` (osEnvMap/newSrvFakeEnv/TestFakeEnvE2E — one responsibility: YOLO_LLM=fake env-gate e2e), `scope_test.go` (TestScopeMatrix — one responsibility: x-yolo-directory scoping 404 matrix) — golden files are not touched (regenerate path unchanged) — effort: S — contract-risk: none (test-only; gate green = verified)
- [refactor-9] P3 internal/server/handlers_misc.go (418) — too-large — "misc" grab-bag spanning five route families (provider+auth-view, project+global config, auth store, agent+command catalog, permission list/reply) — split into: `handlers_config.go` (handleConfigGet/Patch + handleGlobalConfigGet/Patch + mergeWriteConfig — one responsibility: config read/merge/write routes), `handlers_provider.go` (providerEntries/authState/authSnapshot/snapshotStoreLocked + handleProvider/handleProviderAuth — one responsibility: provider list + key-source view + their auth-state recompute), `handlers_catalog.go` (baseAgents/commands/handleAgent/handleCommandList — one responsibility: static agent/command catalog routes); handlesAuthPut/Delete move to handlers_provider.go (same auth-store owner), `handlers_misc.go` then holds only handlePermissionList/handlePermissionReply/askedProps (or rename to handlers_permission.go) — effort: S — contract-risk: none (same-package moves; contract_test.go goldens green = verified)
- [refactor-10] P3 internal/tui/store/store.go:29 — complex-function — `Apply` (105 lines) is one big switch inlining per-event unmarshal + field updates, while the shared `upsertPart`/`applyDelta` do a linear O(M×P) scan per part — extract per-event methods `applySessionUpdated` / `applyMessageUpdated` / `applyMessagePartUpdated` / `applyMessagePartDelta` / `applyPermissionAsked` / … (one responsibility each: fold one event type into the store); Apply becomes a pure type-dispatch switch (≤30 lines) — effort: S — contract-risk: none (display state only; store_test.go + TUI suites green = verified)
- [refactor-11] P3 internal/tool/read.go:117 — complex-function (also duplicated) — `readTool.Run` (88 lines) mixes arg validation, workdir join, stat + dir/binary branching, line streaming, body render and meta build; the display-meta map (preview/truncated/display{...}) is built nearly twice, once for files (:189-201) and once for directory listings (:328-339) — extract: `renderFileBody(fp string, rawLines []string, offset, last, count int, cut, more bool, maxBytes int) (text string, meta map[string]any)` (one responsibility: render the `<path>/<type>/<content>` block + footer + display meta for a file read) and a small `displayMeta(kind, path, preview string, truncated bool, extra map[string]any) map[string]any` helper for the shared meta shape — end state: Run is the ~40-line dispatcher (args → stat → branch → render) — effort: S — contract-risk: behavior (tool output text is model-facing — read_test.go pins must stay byte-identical)
- [refactor-12] P3 internal/tool/shell.go:87 — complex-function — `Shell.Exec` (89 lines) inlines spawn-wait, output-line callback dispatch, marker parse, timeout kill and reap in one goroutine-free body; the two deviation-15/16 pins live in its surroundings — extract: `execTimeout(ctx, st, onLine)` (one responsibility: run one command through the persistent shell, enforce the ctx/timeout kill, return exitCode+out) so Exec becomes arg-check + env + execTimeout — land only alongside another shell.go change (pinned area, deviation 15/16) — effort: S — contract-risk: behavior (shell_test.go + bash_test.go green = verified)
- [refactor-13] P3 cmd/yolo/main.go:105 — entangled-boundary — `buildDeps` (87 lines) builds the entire server dependency graph (dir resolution, DB open, bus, provider registry, permission service, tool registry, env-gated fake selection, config loader) inline in main.go next to CLI parsing — extract to `cmd/yolo/deps.go` (buildDeps + openDB + envMap — one responsibility: wire the in-process server dependency graph from the working directory) — effort: XS — contract-risk: none (same-package; main_test.go green = verified)
- [refactor-14] P3 internal/storage/dao.go (504) — too-large — one file holds ~21 DAO methods across four entity kinds plus the row↔protocol mappers — split into: `session_dao.go` (session CRUD — one responsibility: session rows), `message_dao.go` (message CRUD — one responsibility: message rows), `part_dao.go` (part upsert/list + listPartsBy — one responsibility: part rows; hot paths UpsertPart/ProtocolToPart/PartToProtocol move with it), `permission_dao.go` (permission save/list/reply + AlwaysRules — one responsibility: permission rows), `todos_dao.go` (SaveTodos/GetTodos — one responsibility: todo rows), `part_convert.go` (ProtocolToPart/PartToProtocol/SessionFromRow + nullStr/nullStrPtr/agentOrDefault helpers — one responsibility: row↔protocol mapping) — effort: S — contract-risk: none (same-package moves; storage_test.go green = verified; hot-path DAOs move without code change)
- [refactor-15] P3 internal/tui/app.go:49-51,527-545 (also model.go:45-60, agent.go) — entangled-boundary — the dialog stack stores only a kind (`dialog{kind}`) while the dialogs' live state (modelDlg/agentDlg pointers) is carried on the App root and reconciled by dlgView/handleDialogKey popping the stack on nil pointers — state is owned by the wrong unit: the stack is not self-describing, and a nil-pointer check is used as a dialog-lifecycle signal — end state: dialog items own their payload (`dialog{kind, model *modelDlg, agent *agentDlg}`, created on push in openModelDialog/openAgentDialog, discarded on pop) so `dialogStack` + App need no modelDlg/agentDlg fields and handleDialogKey/dlgView read state from the item directly — effort: M — contract-risk: behavior (dialog open/close/patch flow is teatest-pinned; app_test.go/model_test/agent_test green = verified)
- [refactor-16] P3 internal/llm/anthropic.go:63 — complex-function — `anRequest` (97 lines) builds the Anthropic wire request as one nested map literal (messages, tool_calls, tool schemas, usage/turn options) — extract `anMsg(m llm.Message) map[string]any` and `anTool(td llm.ToolDef) map[string]any` builders (one responsibility each: map one message / one tool definition onto the Anthropic wire shape) so anRequest is the ~30-line envelope assembly — effort: XS — contract-risk: behavior (wire JSON is golden- and test-pinned — anthropic_test.go byte-comparisons must stay green)

## logging rework (0.2.0 — user-deferred 2026-08-22; DONE in 0.2.0, Task 7 / `yolo-326`)
**Status: implemented in 0.2.0** — `feat: slog-based leveled file logger with run id, level env, and new log points` (custom `pinnedHandler` on `<dataDir>/log/yolo.log`, 5 MiB rotate, `YOLO_LOG_LEVEL`, opt-in `YOLO_PRINT_LOGS=1`; new lifecycle/tool/LLM/auth log points; deviations 86/87). The two absorbed findings below are annotated `fixed in 0.2.0` inline.

Not a wave finding: user-reported "not enough data in yolo.log" (the TUI runs that
reproduced the v0.1.3 loops each left exactly ONE line — "turn aborted").
Investigated on `v0.1.4_logging_fix` (branch at `main`, tree clean); deferred to
0.2.0 by user decision 2026-08-22. Design via brainstorming when 0.2.0 scope is
picked (no spec/plan yet by design).

**Diagnosis (verified 2026-08-22):**
- Mechanism is healthy: `internal/log` appends `<RFC3339Z> <info|error> <text>` to
  `<dataDir>/log/yolo.log`, rotates at 5 MiB → `yolo.log.1` (1 generation,
  overwrite); `go test ./internal/log/...` green.
- Coverage is thin (23 non-test log points), almost all on fatal/error
  paths — engine 8 (7 persistence/marshal-failure Errorf + "max tool steps
  reached" Infof; NOTHING on turn/model/tool lifecycle), server 6 (turn aborted
  = info, turn failed, SSE emit, auth load, handler panic, generic server
  error), main 5 (startup/listen failures; `serving on` + `received <sig>,
  shutting down` exist in **serve mode only** — `tuiCmd`, the real-user mode,
  logs nothing at start/stop), permission 4 (all error paths).
- The v1 plan (Task 30, line 5998) pinned exactly this scope — "server
  handlers + engine errors + cli startup use it (no print)" — so the thinness
  is by-plan, not a port gap; lifecycle/observability logging was never in
  v1 scope.
- No logging at all in LLM drivers, tool execution, storage, config or auth
  resolution; no levels (Infof/Errorf only).
- Upstream parity reference: `packages/core/src/observability/logging.ts` — leveled
  file logger to `Global.Path.log/opencode.log`, `OPENCODE_LOG_LEVEL`
  (DEBUG/INFO/WARN/ERROR, default Info) + stderr logger. OTEL spans on LLM calls stay
  excluded (zero telemetry, root principle 1 / spec §1).

**Proposed 0.2.0 scope (TBD in brainstorming; constraints are fixed):**
- Levels on the existing line format via `YOLO_LOG_LEVEL` env
  (DEBUG/INFO/WARN/ERROR, default INFO), mirroring upstream semantics.
- New sinks: agent loop (turn lifecycle, model rounds, tool start/end + exit,
  aborts), LLM drivers (request start/end + status/latency + errors — local file
  only), tool execution (bash command + result/truncation), storage open/migrate,
  config load, auth resolution source (keys NEVER logged — golang-security).
- `tuiCmd` lifecycle lines (serving on / shutdown) for parity with serve mode.
- Absorb related deferred findings: [security-5] log.go:62 CWE-117 newline forge;
  [troubleshoot-2] engine.go `Deps.Log` nil=no-op (re-verify when picked up —
  `Logger.write` has a nil-receiver guard, so a nil `Deps.Log` may already be a
  safe no-op and the finding may be stale).
- Constraints: zero telemetry (local file only, nothing remote); no new deps
  (stdlib logger stays); unit tests never hit the network (fake driver for engine e2e).

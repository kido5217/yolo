# Yolo — Progress & Status (session checkpoint)

**Updated:** 2026-08-18 (T10–T23 done, **M5 COMPLETE** + Tasks 22–23 (M6 start) done — active: Task 24)

## Where we are

Plan approved by user ("LGTM"). Executing inline on branch `plan`, one task at a time, strict 5-step TDD per plan, commit per task. **M0, M1, M2, M3 COMPLETE** (Tasks 10–14: glob+permission, tool framework + truncation + read, write + edit, glob + grep tools, bash persistent shell + todowrite). M4 in progress: **Task 15 DONE** (internal/session prompt builder + 14 verbatim prompt files + message.agent DDL fold-in), **Task 16 DONE** (internal/llm/fake scripted driver + session engine core — single text turn end-to-end + tool-result history replay), **Task 17 DONE** (engine × permission — ask/deny/always, wildcard tool hiding, sliding doom loop, aborted path), **Task 18 DONE** (engine lifecycle — bounded jittered retry, abort cascade, per-turn max-steps, context overflow, 409 busy), **Task 19 DONE** (internal/server — Deps/DI shape, `x-yolo-directory` scoping, session CRUD + message + send/409 + abort + status, command, SSE, error envelope, fake auto-turn + FromScript, `auth.LoadFrom/SaveTo` + `config.LoadGlobal/SaveGlobal` path seams). **Task 20 DONE** (remaining endpoints — 12 routes: provider list/auth-state with per-request recompute + boot-lifetime auth store, config get/patch (scoped, project `yolo.jsonc` merge-rewrite) + global get/patch (`<Home>/yolo/yolo.jsonc`), auth PUT/DELETE 204, agent locked descriptions + `Custom agent.` stubs, command locked 5, permission scoped list + reply with `parkAsk` test seam). **Task 21 DONE** (`internal/server` contract golden tests — 14 golden envelopes via `-update` normalizer, SSE exact-ordering, fake-env e2e, scoping matrix; see deviations 41–42). **Task 22 DONE** (M6 start: `internal/tui/client` — wire client for all 18 routes + `Events` SSE reader with injectable backoff; `internal/tui/store` — `Store` + `Apply` per event type with current-session scoping; M5 harness moved to `internal/server/testutil` exported; see deviations 43–45). **Task 23 DONE** (M6: `internal/tui` root app — `App`/`NewApp`/tea plumbing + SSE event pump cmd + hydrate cmds (home list / session detail / resume not-found→Quit), home route with locked layout (28-`─` divider, `▸` cursor, `·`-joined session lines, help footer), relative time, up/down wrap + enter open/create + `n` create + `/help` dialog + ctrl+c quit-confirm, dialogStack (quit/help), session-route stub (esc back), `EventMsg`/`HydrateMsg` exported for teatest, `record`/`Cmds` test hook; charm deps pinned direct in go.mod — teatest via github module path, see deviations 46–47). Active is **Task 24: TUI session viewport + streaming render**.

## Resume instructions (next session)

1. Repo: `/home/kido/network/projects/yolo` (branch `plan`). Plan: `docs/superpowers/plans/2026-08-17-yolo-go-port.md` (6020 lines; read the task's slice before executing it). Spec: `docs/superpowers/specs/2026-08-17-yolo-go-port-design.md`.
2. Continue at the Active task below. Per task: Step 1 failing test → Step 2 confirm FAIL → Step 3 minimal impl → Step 4 `go vet ./... && go test ./...` PASS → Step 5 commit with the plan's message.
3. LSP diagnostics for `cmd/yolo/main.go` re `loadStore`/`store` are STALE — the file builds and all tests pass.
4. Zen catalog CDN blocks python-urllib (403); fetch with curl + browser UA.
5. Golang skills: 15 project skills in `.agents/skills/` (samber/cc-skills-golang, hashes in `skills-lock.json`) — invoke the relevant one(s) per task: `golang-naming`/`golang-code-style` for new code, `golang-error-handling` for error flow, `golang-testing` for tests, `golang-concurrency` for concurrent code, `golang-safety` for defensive review, `golang-data-structures`/`golang-performance`/`golang-benchmark` for hot paths, `golang-database` (storage pkg), `golang-security`, `golang-troubleshooting` (bugs), `golang-design-patterns` (interfaces/DI), `golang-cli` (cmd/yolo), `golang-refactoring`.

## Completed work this session

| Item | Where |
|---|---|
| Requirements + 7 design sections approved by user | spec (§1–§8), commits `f54991c`, `cec9a8f` |
| Spec written, source-verified, committed | `docs/superpowers/specs/2026-08-17-yolo-go-port-design.md` |
| Implementation plan: 30 tasks over M0–M8 | `docs/superpowers/plans/2026-08-17-yolo-go-port.md` |
| M0 Task 1: module bootstrap, cmd/yolo dispatch, server /global/health | `e2d2565` |
| M1 Task 2: protocol DTOs (id/session/message/part/event/provider/agent/config) | `742c652` |
| M1 Task 3: config discovery, JSONC, deep merge, env substitution | `c1a0b5c` |
| M1 Task 4: auth.json store, key resolution, `yolo auth` CLI | `6562ed2` |
| M1 Task 5: storage (SQLite schema v1, DAOs, aggregates, ProtocolToPart) | `ec6ea45` |
| M1 Task 6: in-process event bus | `de97df8` |
| M2 Task 7: llm Driver interface + OpenAI chat-completions SSE driver | `03fdb36` |
| M2 Task 8: Anthropic Messages SSE driver (+ shared test helpers) | `765293c` |
| M2 Task 9: provider registry — kido live/fallback, zen catalog filter+cache (frozen fixture; 91/64/57/42+15/7 gate verified) | `62edde5` |
| M3 Task 10: internal/glob matcher + internal/permission (Evaluate findLast, Hidden, DoomLoopDue, build/plan/yolo matrices, ask/reply Service with park/persist/cascade) | `e4f23cd` |
| M3 Task 11: internal/tool — framework (Limits/Env/Output/Tool/Registry/Visible/SchemaFor), Truncate (upstream tail() port, UTF-8-boundary cut), read tool (file/dir/binary/miss-suggest, desc sha256-pinned) | `7130344` |
| M3 Task 12: internal/tool — write (MkdirAll, meta {added,removed} via LCS line diff in write.go) + edit (exact-match replacer, per-file sync.Map mutex per upstream Semaphore, upstream error strings verbatim, empty-oldString→create path kept); desc/write.txt + desc/edit.txt byte-verbatim (hash-verified) | `3ad74dc` |
 | M3 Task 13: internal/tool — glob (WalkDir, hidden entries skipped, sorted, limit 100, missing/`File` path → error `glob path must be a directory: {search}`) + grep (RE2, hidden-skipped walk per Step-3 deviation note, >10MB + NUL-binary skips, include via `glob.Match` on rel, limit 100 with pinned "Found N matches (more matches available)" + truncation note, file root → its dir, missing root → "No files found"); desc/glob.txt + desc/grep.txt byte-verbatim (hash-verified). In-plan contradictions resolved per last-stated call: grep skips dotfiles (Step-3 deviation note overrides "dotfiles searched"); glob missing dir → explicit error (spec parenthetical overrides "empty result") | `cc29f28` |
 | M3 Task 14: internal/tool — persistent-session `bash` (shell.go: one `bash --norc --noprofile` per session, Setpgid, marker `echo __YOLO_END_{n}_$?_$(pwd | base64 -w0)` + reader `^__YOLO_END_{n}_(\d+)_(\S*)$`, exit code + cwd from marker, single output pipe with stdout+stderr on its write end, SIGKILL process group on timeout/abort + respawn, 10MB in-memory guard) + todowrite (replace-whole-list persist, `N todos` title = non-completed count, 2-space-indented JSON text, `Meta{todos}`, status/priority validation); storage migration v2 `todo` table + `SaveTodos`/`GetTodos` + `protocol.Todo`; `Env` gains `Storage *storage.DB` + `SessionID string`; `Registry()` = 7. desc/bash.txt (rendered from upstream v1 template, workdir section dropped, /tmp/yolo pinned) + desc/todowrite.txt byte-verbatim (hash-verified). See deviations 15–16 (marker colon; stderr-on-read-end) | `ae0ff27` |
 | M4 Task 15: internal/session — prompt builder: 14 upstream prompt files byte-verbatim (13× `session/prompt/` minus `plan-reminder-anthropic.txt` + `agent/prompt/title.txt`) guarded by 14 sha256 `promptPins`; `FamilyPrompt`/`FamilyName` 8-way first-match family selection (pinned), meta `{{MODEL_NAME}}` → Muse Glimmer/Muse Spark; `EnvBlock` (pinned format, 2-space-indented `<env>`, git-detect `git -C dir rev-parse --is-inside-work-tree` 2s timeout, cached per dir, JS-toDateString date `Mon Jan 02 2006`); `BuildSystemPrompt` pinned signature = [family, env, AGENTS.md walk-up nearest-wins ≤32 hops]; test seam `buildSystemPromptForTest(dir, model, providerID, instructionPaths)`; `PlanReminders` (current=plan → plan.txt; build after last assistant plan → build-switch.txt). PLAN FIX folded in: `message` table gains `agent TEXT NOT NULL DEFAULT 'build'` (still migration v1) + `MessageRow.Agent` (normalized on write) + `TestMessageAgentRoundTrip`. See deviations 17–18 (plan test fixes; env-block expectation rewrite) | `a7e604f` |
 | M4 Task 16: internal/llm/fake — scripted `llm.Driver` (`Turn{Parts,Err}`, `Driver{Turns,TitleTurns,ReqLog,Requests()}`, title requests [first system msg starts with titlePrompt's "You are a title generator"] draw TitleTurns, missing turn = empty stream, ctx-aware final error part) + internal/session engine: `Deps{DB,Bus,Prov,Perm,Tools,DataDir,Cfg,Drivers,Clock}`, `New`, `Send` (persists user msg+text part, busy flag → `ErrSessionBusy`, spawns turn goroutine, schedules one-shot title gen when title=="New session" && no assistant yet), `Abort`, `Status() string` (see deviation 19), `Close` (per-session bash shell); turn = N model rounds (`maxToolRounds=50`) under `runTurn` (busy→idle `session.status` bookkeeping, `onDone` exactly once); LOCKED history mapping implemented verbatim (system entries → separate RoleSystem msgs + config instructions[] appended after; user join + PlanReminders on LAST user; assistant text-only + ToolCalls from completed/error tool parts; each tool part → one RoleTool msg after its assistant; empty assistant skipped; request re-appends last user msg when tail isn't user — round-2 tail = [user,assistant,tool,user]); tool exec inline (`executeTool`): args parse (fake convention: `Part.Text` carries args JSON when `Args` empty) → patterns/external → running part → external_directory gate (paths outside project dir) → `Perm.Ask` with `DecisionPre` → `toolEnv{Dir,Shell(lazy per session),Limits from cfg.tool_output,Storage,SessionID}` → completed/error part; text/reasoning part bookkeeping (create-on-first-delta emits part.updated + part.delta, subsequent deltas part.delta only, finalize single part.updated with end — "exactly one final part.updated" holds); tokens/cost persisted per assistant msg (USD per 1M); title gen = best-effort side call w/ 30s timeout, first line ≤50 runes → `session.updated`. TEST SEAMS folded in: `provider.NewWithSeams(ctx,dataDir,seam)` (+ `Registry.seam` + `Resolve` hook) for the offline kido/q catalog; `session.TitlePrompt()` exported. `go vet` clean; `go test -race ./internal/...` all 13 pkgs pass. See deviations 19–21 | `34c294e` |
 | M4 Task 17: internal/session — engine × permission integration: NEW `policy.go` (`RulesetFor(sessionID)` = builtins(agent,dataDir) [unknown agent → build matrix] + config `ParsePerms(cfg.Permission)` + `db.AlwaysRules`, cfg-load error degrades to no config rules; `rulesetForRow`; `VisibleToolsFor` = `tool.Visible`; `toolSchemaList` sorted-id schemas) + `executeTool` LOCKED gate order: (1) doom check — per-TURN `[]permission.CallKey` (resets each Send), `callKeyHash` = sha256 of canonical sorted-key JSON args, sliding-3 window (`DoomLoopDue`) asks action `doom_loop` resource `[tool]` **before** the part goes "running"; (2) hidden guard — `permission.Hidden(rulesetForRow)` → part error `tool not available`; (3) external_directory gate; (4) core Ask with Patterns/Always. Error mapping: Deny → `permission rejected`; ctx cancel while parked (Abort) → `aborted`; doom Deny adds metadata `{reason: doom_loop}`; model continues after every gate failure. `buildRequest` now re-reads the ruleset per round via `toolSchemaList`. TEST SEAMS: harness gains `t`/`svc *permission.Service`/`cfgPermission []protocol.Rule`/`replies chan string` + `replyWatcher` (bus subscriber answering `permission.asked` FIFO from the queue, 3s no-reply t.Fatal, exits on cleanup `done` channel), `queueReplies`, `setAgent`; `build()` wires `Deps.Perm: h.svc` + `cfgLoader()` Cfg closure. Tests: deny-stops-tool-not-turn, always-persists-and-skips-next, hidden-tool-not-sent, doom-third-identical-asks (+ ordering: doom ask precedes the call's running part), ABORT-during-ask (self-authored 5th, deviation 25) | `bfd1184` |
 | M4 Task 18: internal/session — engine lifecycle: `Deps.Backoff func(attempt int) time.Duration` (nil → `defaultBackoff` = 1s × 2^(attempt-1) × uniform jitter [0.8,1.2], `math/rand`); `runRound` restructure — assistant row created BEFORE the first stream attempt (failed rounds finalize an empty message); pre-stream retry loop (≤ `maxRetryAttempts`=4, `llm.IsTransient` only, per attempt emits `session.status` retry{attempt,message,next}; ctx always checked before the overflow regex since `context.Canceled` matches `(?i)(context|…)`); mid-stream `p.Err`/stream loss → NO retry, partial text kept, error as synthetic note part, `onDone(wrapped err)`; per-turn tool-call budget `maxToolSteps`=50 owned in `runTurn` (`toolCalls *int` threaded to `runRound` — the `maxToolRounds=50` model-round cap stays as backstop), 51st+ calls of the final stream dropped (not persisted/executed), idle + `onDone(nil)` + slog; context overflow — usage `input > model.Context` after a round, or pre/mid-stream API error matching `(?i)(context|tokens|too long|exceeds)` → synthetic text part with the pinned overflow message, idle, `onDone(nil)`; synthetic parts persisted via `storage` `synthetic` flag (NEW round-trip in `dao.go` for text/reasoning) and EXCLUDED from `messagesFor` history replay (model never sees them); abort — `Abort` cancels turn ctx, running tool's Run error mapped to `aborted`, idle, `onDone(context.Canceled)` NON-fatal (slog info only); 409 = existing `ErrSessionBusy`. Tests (NEW `lifecycle_test.go`): transient-retry-succeeds (2 retry events via idle-event barrier + eventCount), give-up-after-4 (onDone(err) asserted), midstream-no-retry, abort-mid-turn (running tool → error `aborted`), max-tool-steps (51 unique-pattern globs → exactly 50 persisted tool parts), overflow-hard-stop (input 100001 > ctx 100000 → pinned note, 1 request, no round 2), concurrent-send-409 (`slowDriver` wrapper-sleep seam, busy between Sends), + `lifecycle_internal_test.go` `TestDefaultBackoffBounds` (500 iters × attempts 1–4 within [0.8,1.2] × base) | `aa26250` |
 | M5 Task 19: internal/server — pinned `Deps{DB,Bus,Engine,Prov,Perm,Config,WorkDir,Dirs}` + `New(d Deps) http.Handler` (drops the M0 `New(workDir, ...Opt)`/`Scope` shape — deviation 30) + `NewServer`/`ServeHTTP` (listener optional: `Start/Addr/Close`, used by cmd), Go 1.22 route table = M0 `/global/health` + the 13 plan-pinned routes (14 patterns) + 4 per-method catch-alls → 404 envelope; `scope` = PathUnescape + `os.Stat` isDir → 400 on bad escape/non-dir (workdir fallback unvalidated); envelope `{"error": protocol.Error{Message,Data}}` + recover middleware; `decode` tolerates empty body; `/project/current` reuses the M1 hex `projectIDFromDir` via NEW export `storage.ProjectID` (deviation 32); session handlers: `GET /session` (ListSessions, zeros cost/tokens — aggregates come from `Session(id)`), CRUD (create 201 defaults `New session`/`build`/default model, patch pointer-partial, delete → `Engine.Close` + `session.deleted` → 204), out-of-scope session → 404; `GET /session/{id}/message` (message+parts → `MessageWithParts`); `POST …/message` → 202 `{"message_id"}` (400 empty, 404, 409 `ErrSessionBusy`), send passes `context.Background()` (turn outlives request; engine request-ctx cancel → abort cascade); `POST …/abort` blocks until `Status(id)==idle` (≤2s poll) then `{"aborted":bool}` (deviation 33); `GET /session/status` → `{"sessions":{id:status}}` non-nil; `POST /session/{id}/command` — `/new` → new default session + `session.updated` + `{"session_id"}`, other known `{/help,/model,/agents,/exit}` → `{"handled":"client"}`, empty/unknown → 400; NEW `protocol.CommandResponse`; NEW `GET /event` SSE (`data: <json Event>\n\n` = `{"id":"evt_…","type":…,"properties":…}` per bus event; 200+flush before subscribe; flush per frame; client-ctx / bus-close end stream). TEST SEAMS: fake gains `Turn.Auto` (sticky auto-turn) + `AutoText()` + `Turn.Delay` + `SetDelay` + `FromScript` (deviation 35 — replaces T18 dev-26 `slowDriver` for server-level slow-turn tests); `provider.NewStaticForTest()` (kido/q default, `KeyRequired:false, KeyLoaded:true` + opencode/gpt-5-nano); `auth.LoadFrom/SaveTo`, `config.LoadGlobal/SaveGlobal` (delegation per M5 4977; JSONC comment loss = dev 7). Tests: plan's 5 verbatim (plan-buggy send test fixed — deviation 31; harness pseudocode filled — real httptest + `storage.Open` after parent MkdirAll + `permission.New` + `tool.Registry` + zero `config.Loader` + real Clock + `Drivers{"kido":fake}`) + 3 self-authored (deviation 34). Gate green incl. `-race -count=1 ./internal/server ./internal/session ./internal/llm/...` | `22328d4`
  | M5 Task 20: internal/server remaining endpoints — NEW `handlers_misc.go` + `handlers_misc_test.go`, 12 routes added (all but reply/config-scoping unscoped per M5; `PUT /{tail...}` catch-all added → 404 envelope): `GET /provider` (SCOPE-D — deviation 38: pinned test scopes it, so config providers resolve per request dir) = registry list + config-defined providers (included on `len(models)>0` alone — deviation 39, test writes `base_url` not the locked `baseURL` tag), each with per-request auth recompute (env → auth.json store → config `apiKey`, mirrors `auth.ResolveKey`) and env fallback `[EnvName(id)]`; `GET /provider/auth` = {id: {key_required, env, status, source}} (source = env/auth.json/config/none; kido `not-required`); `GET /config` (scoped, marshaled merged `*protocol.Config`); `PATCH /config` = deep-merge partial into project `yolo.jsonc` (JSONC read via `config.UnmarshalJSONC`, 2-space rewrite, comments lost = dev 7), returns merged; `GET /global/config` = `config.LoadGlobal(<Home>/yolo)`; `PATCH /global/config` = map-level merge-rewrite of `<Home>/yolo/yolo.jsonc` (deviation 37 — plan test said `global.jsonc`; M5 4977 lock `yolo.jsonc` + T3 dev 2 stand); `PUT/DELETE /auth/{providerID}` 204 → boot-lifetime in-memory `auth.Store` (NEW Server fields `authMu/authPath/authStore`, init in `build` from `<Dirs.Data>/auth.json`, `initAuth`/`authPath`) persisted via `auth.SaveTo`; `GET /agent` locked verbatim descriptions (build/plan/yolo) + config-defined ids → name only `Custom agent.` (all `Mode:"primary"`); `GET /command` locked 5 (`/help` show help, `/new` new session, `/model` pick model, `/agents` pick agent, `/exit` quit, Hints []); `GET /permission` (scoped) = `DB.ListSessions(dir)` × `Perm.Pending(rowID)` → `PermissionAskedProps` (tool/agent into Metadata); `POST /permission/{requestID}/reply` — body validated BEFORE id lookup (LOCKED: bad response 400 not 404), `errors.Is(permission.ErrNoPending)` → 404, ok → 204. Package edits: `permission.Service` gains exported `ErrNoPending` (wrapped by `Reply`); `provider.FromConfig(id, oc)` export (config-provider wire mapping shared by the handler); `provider.NewStaticForTest(seed ...Model)` — opencode models seeded with 2-model zen catalog (`claude-opus-4-7` anthropic + `gpt-5-nano` openai), seed replaces them. TEST SEAMS (server_test.go): `srv.permSvc`, `parkAsk(sessionID, action, resource)` (goroutine `Ask` + 5ms HTTP poll of `GET /permission` until the RequestID appears — 2s budget — preventing the park-race where the reply 404s before the ask parks), `writeCfg(t, dir, jsonc)`. Tests: plan's verbatim 7 + self-authored `TestProviderAuthEndpoint` (deviation 40). Plan test bug fixed in place — `TestAgentAndCommand` declared `resp` without asserting it (compile error); dropped the unused binding. Gate green first try incl. `-race -count=1 ./internal/server ./internal/session ./internal/llm/... ./internal/permission` | `c6e1d2c`

 | M5 Task 21: internal/server contract tests — NEW `contract_test.go` (4 groups, committed WITH the generated goldens in one commit) + `testdata/golden/*.json` (14 files, generated ONCE via `go test ./internal/server/ -run Golden -update`, hand-inspected, zero-leak scanned: no raw `ses_/msg_/prt_/prj_` ids, no temp paths, no epoch ints). Normalizer: decoded-JSON walk, key-sorted maps, generated ids → `SES1`/`MSG1`/`PRJ1`/… (deduped per concrete id via `seen` map), project dir → `<DIR>` / basename → `<DIRNAME>` / substring → ReplaceAll, integer `float64 >= 1e11` → `<T>` (1e11 keeps context/output/token/cost ints literal). `-update` = package-level `flag.Bool`, test `TestGoldenResponses` (matches `-run Golden`). (1) 14 golden subtests, one canonical request each (send-based ones use explicit session title `"Golden"` to skip the one-shot title-gen side request — deviation-21c trap; the title goroutine also races `ReqLog` in the e2e). (2) `TestSSEOrdering`: EXACT faithful 9-frame order for one auto-text turn — user `message.updated` → user `message.part.updated` → `session.status busy` → assistant round-start `message.updated` → assistant `message.part.updated` (create) → ≥1 `message.part.delta` → final `message.part.updated` (full text, `time.end` set) → assistant final `message.updated` → `session.status idle` (last frame) — asserted by first-3-by-index + relative-order chain (deviations 41–42: the plan's "busy first" pin is wrong vs engine AND upstream v1.18.18 `prompt.ts` createUserMessage-before-busy). Determinism: SSE subscribes AFTER the 200 handshake flush (deviation 36) → NEW `bus.SubscriberCount()` (test support, production path untouched) + harness `waitSubscribe(t,n)` polled before the send. (3) `TestFakeEnvE2E`: in-process `os.Setenv(YOLO_LLM=fake, YOLO_FAKE_SCRIPT)` + `t.Cleanup` restore → `server.FakeFromEnv(osEnvMap)` (the real M5 env gate) → full `session.New`+`server.New` stack; 2-turn script (turn 1 = `read` tool call `{"filePath":"note.txt"}`, turn 2 = text) with `note.txt` seeded in the WORK DIR (the read tool resolves relative paths against the session dir — first version seed-ed a throwaway tempdir and the tool part persisted `error: File not found`, fixed); asserts persisted messages+parts (2 users, completed read tool part with note in output, closing text) and history replay via `drv.Requests()`: request[1] (send-1 follow-up) and the last (send-2) each carry a `RoleTool` message containing the note; send-2 request has 2 `RoleUser` (start/end). (4) `TestScopeMatrix`: absent header → WorkDir (`/path` + workdir-scoped session list) + the 7 id-scoped routes (plan said 8 — deviation 42) all 404 for a foreign-dir session id, owning dir resolves. Gate: `go vet ./... && go test ./...` green; new tests `-count=5` stable; `contract_test.go` gofmt-clean (pre-existing gofmt-dirty files untouched). | `d67787e` |
| M6 Task 22: internal/tui client + SSE reader + Store + testutil export — NEW `internal/tui/client/client.go` (`Client{Base,Dir,HC,Backoff}`, `New`, one `do(ctx,method,path,in,out)` helper: dir header via `url.PathEscape` (wire-faithful: upstream SDK `encodeURIComponent`, server `PathUnescapes`), error-envelope decode, status→sentinel mapping `404→ErrNotFound`/`409→ErrBusy`/`400→ErrBadRequest`; all 18 client methods per plan: Health, ListSessions, CreateSession, GetSession, PatchSession, DeleteSession, ListMessages, SendMessage(→message_id), Abort(→bool), Command(→CommandResponse), Status(→map[string]string — deviation 43), ListProviders, GetConfig, PatchConfig, GlobalConfig(patch|nil), Auth(PUT/DELETE), ListAgents, ListCommands, ListPermissions, ReplyPermission, Events) + `event.go` (goroutine loop `GET /event` → `data: <json>\n\n` frames → `protocol.Event` → buffered chan(4); on close/error → `backoff(n)` = min(30s, 1s<<n), field-injectable `Backoff` for tests; ctx done → chan closed, nothing emitted after) + NEW `internal/tui/store/store.go` (`Store{Sessions,Current,Messages,Providers,Agents,Commands,Config,Status,Pending,Conn,LastHydrate}`; `Apply` switch: message.* only for Current session — part deltas synthesize missing part/message (text/reasoning→Text, input→ToolState.Input["input"] accumulate, unknown part type inferred from field); session.updated upserts list + replaces Current on match; session.deleted clears; session.status sets Store.Status for current only; permission.asked dedup-append by ID; permission.replied remove by RequestID; Conn NOT touched by Apply) + M5 harness moved to NEW `internal/server/testutil` (exported `TestServer{DB,Bus,Eng,Fake,PermSvc,Dir,Home}` embedding `*httptest.Server`; `Boot`, `BootWithDriver`, `WaitSubscribe`, `FakeDelay`, `ParkAsk`, `WriteCfg`, `Req`, `SSEFrame`(+`.String`), `SSEReader`/`SSEConnect`/`Frame`) with mechanical reference renames in the 3 server test files (deviation 44; no behavior change). Tests (green incl. `-race -count=5`): `TestClientScopingAndErrors` (dir header asserted escaped `%2Fabs%2Fdir` — deviation 45, plan sketch had expected the raw value; 409→ErrBusy, 404→ErrNotFound, 400→ErrBadRequest; route echo), `TestEventsDecodeAndReconnect` (conn 1 emits evt_1 then handler returns → server closes → client backs off and reconnects → conn 2 emits evt_2; asserts order + reconnect count, injectable Backoff 10ms), `TestApply` (9 subtests, one per event type, on the pinned seed: 1 current session + 1 message + 1 part) + `TestApplyIgnoresOtherSessions` (foreign message.updated/status leak-free; current session.updated updates list + Current title). TUI import rule verified: non-test `internal/tui/*` import only `internal/protocol` + stdlib (no `internal/server`). `EventMsg`/`HydrateMsg` carriers stay for Task 23 (LOCKED: defined in tui root; client returns only the chan). | `4a7638c` |
| M6 Task 23: internal/tui root app + home route — NEW `internal/tui/app.go` (`App{*client.Client, store, route, cur, home, sess, prompt, dlg, toasts, lastErr}` + tea plumbing; `NewApp(c, s, startSessionID)` (""=home, else resume route set), `Init` = Batch(hydrateCmd, eventPump); `eventPump()` tea.Cmd blocking on the Events chan → `EventMsg` / chan-close → `connLostMsg` (no re-arm — the client reconnects internally with backoff); hydrate cmds (home → `ListSessions`; session → `GetSession`+`ListMessages`; `ErrNotFound` → visible `! session not found: <id>` line + `Quit`); `sessionCreatedMsg` → `putSessionFirst` (head upsert, deduped later by SSE `session.updated` via `store.Apply`) + open + re-hydrate; KEY DISPATCH `handleKey` = dialog > session > home: home up/down wrap over 1+N visible (N = min(50, len)), enter (row 0 → create cmd; row k → open + hydrate cmd), `n` → create cmd, ctrl+c → quit dialog (y/enter/ctrl+c quit; n/esc close; unknown stays), printable input accumulates the `/help` buffer (match opens help dialog, mismatch clears); session route: esc → home + list hydrate; `View() tea.View { return tea.NewView(a.view()) }` over pure `a.view() string` (v2 Model deviation 47); `Close()` cancels the pump ctx; `record` bool + `Cmds []tea.Cmd` test hook via `emit`), `home.go` (`homeModel{cursor, buf, now}`; `relTime` locked <60s/`5m`/`3h`/`4d`; `lineContent` = `title · provider/model · rel` (title only when Model nil); `render` = locked layout: `Yolo` / 28-`─` divider / rows `  ▸ <content>` (cursor) `  <content>` / divider / `↑/↓ move · enter open · n new · /help`), `style.go` (locked lipgloss v2 names `title` bold, `divider` fg240, `cursor` bold, `dim` fg245, `errRed` fg196, `okGreen` fg42, `toolRow` fg250 — full-line styling keeps assertion substrings contiguous, whitebox test strips SGR). Tests: `home_test.go` (whitebox `package tui`: `TestRelTime` table, `TestHomeRenderLockedLayout` exact 8-line string, `TestAppHandleKeyHome` — cursor wrap, enter open/create, `n` cmd, ctrl+c dialog y/esc, `/help` typing) + `app_test.go` (blackbox `package tui_test` via testutil escape hatch: `TestHomeRendersListAndNewSession` — real stack + teatest v2, assert `Yolo`+`New session` → API create "Hello" → `Send(tui.HydrateMsg{})` → assert `Hello · kido/q`; `TestResumeMissingSessionExitsWithError` — app self-quits on 404 resume, output has `session not found`). Gate green incl. `-race`. See deviations 46–47 (teatest github module path; bubbletea v2 `View() tea.View`/`Quit()`-is-Msg + teatest v2 consumed-output-stream API). | `19e937d` |

## Plan resolutions & flags to raise at handoff (severity)

1. **important** — teatest: spec's `charm.land/x/exp/teatest` is the v1 module and cannot build a v2 TUI; plan pins `charm.land/x/exp/teatest/v2` v2.0.0-20260816001655-68d539dca504 (dev-only).
2. **important** — spec DDL lacked per-message cost/tokens; plan adds `message.cost REAL` + `message.tokens TEXT` (Task 5) and session-level aggregates at read time (Task 19).
3. **important** — spec DDL lacked todo persistence; plan adds migration v2 `todo` table + DAOs + `protocol.Todo` (Task 14).
4. minor — `title.txt` actually lives in `agent/prompt/` (spec said `session/prompt/`); Task 15 embeds 14 prompt files (13 session + title).
5. minor — config `agents` map vs spec's `agent` string ambiguity resolved to `agents` (Task 3/20); custom agents v1 = permission merge + description stub.
6. minor — SSE frames include `id` beyond the spec envelope example (type+properties).
7. minor — JSONC comments are not preserved when config PATCH rewrites the file.
8. minor — v1 keymap: pgup/pgdn scroll viewport, `\`+enter newline (spec's ↑/↓ viewport scroll replaced; noted in /help text).
9. minor — plan's LOCKED doom-loop semantics (Task 17, flagged by the plan itself): the 3-identical window **slides** — any call whose two immediate predecessors are identical (same tool + deep-equal args) triggers a `doom_loop` permission ask; a "once" reply does NOT extend the exemption, so a run of N identical calls asks at every call from the 3rd onward (N-2 asks total). Matches spec §4.5 "last 3 tool parts are the same tool with deep-equal inputs".

## Key verified facts (so they don't get re-litigated)

- Permission engine = port of `packages/opencode/src/permission/index.ts` + matrices in `agent/agent.ts` (build/plan/yolo verbatim in Task 10).
- Doom loop = sliding 3-identical window; wildcard-deny hides tool iff last matching rule is `*` deny; write+edit both map to permission `edit`.
- Pinned deps: `charm.land/bubbletea/v2` v2.0.8, `charm.land/lipgloss/v2` v2.0.6, `charm.land/bubbles/v2` v2.1.1, `modernc.org/sqlite` v1.56.0, `tidwall/jsonc` v0.3.3; dev-only `teatest/v2`.
- Module `github.com/kido5217/yolo`, Go ≥ 1.25 (installed 1.26.5).
- Single deliberate wire deviation: `x-yolo-directory` header.
- Test gating: unit tests never hit network; `YOLO_LLM=fake` + `YOLO_FAKE_SCRIPT` selects the scripted fake driver (wired in Task 19, e2e in Task 21); zen fixture gate = 57 models (42 openai + 15 anthropic, 7 google excluded).
- TUI import rule: non-test files under `internal/tui/` import only `internal/protocol` + `internal/tui/*`; `_test.go` may use `internal/server/testutil` (escape hatch). Enforced by Task 29.

## Active

**Task 24 (M6): TUI session viewport + streaming render.** Plan slice: lines 5844–5872 (read the whole slice before executing; pinned commit: `feat: tui session viewport — streaming, reasoning, tool rows, expand, abort`). Files: Create `internal/tui/session.go` + `internal/tui/session_test.go`. Viewport = `bubbles/v2/viewport` over rendered message blocks. LOCKED focus: **prompt always focused** (T25 lands it; at T24 the session stub stays esc-back-only) — scroll keys `pgup/pgdn` only; ↑/↓ = prompt edit (v1 simplification, noted in /help). Block render per message (LOCKED layout): user verbatim; `▸ think` collapsed / `▾ think` + dim indented text (toggle `t`); tool lines `✓ <tool> <title>` (state.Title; empty → first input arg stringified or callID prefix 8), running `▶ <tool> <title>`, error `✗ <tool> <error>` (first line, red); expand adds 2-space-indented state.Output (≤40 lines, tail) or state.Error; `e` toggles per part; assistant text streams on `message.part.delta`; auto-follow last message when busy AND cursor-at-bottom (user scroll-up pauses; `pgdn` to bottom resumes); abort: `esc` while busy → `client.Abort` (toast on error — toast stubs exist at T29; at T24 render an error line if needed); `message.error`/`session.error` → red `! <message>` line. Step 1 tests: (a) pure render — fixture Store (user msg; assistant msg with text + reasoning + 3 tool parts running/completed/error; expanded states) → expected multi-line string (inline golden); (b) teatest streaming — `ts.Fake` 2-turn script `[text "thinking", tool read], [text "done"]`, open session, type+enter "do it", `WaitFor` `✓ read` + `done`, final frame auto-scrolled (last line `done`); (c) `e` expand/collapse line count. Reuse T23 building blocks: `App.sess sessionModel` is the placeholder to flesh out; keys dispatch through `handleKey` → `handleSessionKey` (extend, don't rebuild); styles `okGreen`/`errRed`/`toolRow`/`dim` exist in style.go; Store already folds deltas (`Apply` synthesizes parts). Teatest v2 gotchas (deviation 47): output stream is consumed by `WaitFor` reads — assert via `WaitFor`s, not a final-frame re-read; model is already a `tea.Model` (`View() tea.View`).

**Task 23 (M6, DONE this session — `19e937d`): TUI root app + home route.** Plan slice: lines 5770–5841. Files: NEW `internal/tui/{app,home,style}.go` + `{app,home}_test.go`. `App` root model (locked struct shape) + `NewApp(c, store, startSessionID)` + `Init/Update/View() tea.View`; SSE pump = self-re-arming tea.Cmd over `client.Events` (`EventMsg` / `connLostMsg` — client reconnects internally); hydrate cmds (home list / session detail / resume `ErrNotFound` → visible error + Quit); home route locked layout (exact 8-line render, 28-`─` divider) + locked keys (up/down wrap, enter, `n`, `/help`, ctrl+c quit-confirm) + `dialogStack` (quit/help) + session-route stub (esc back); `EventMsg`/`HydrateMsg` exported for teatest; `record`/`Cmds` lock hook. Blackbox teatest v2 test against the real stack (`testutil.Boot`) + whitebox pure/key tests. Gate green incl. `-race`. See deviations 46–47.

**Task 17 (M4, DONE this session — `bfd1184`): engine × permission — ask/deny/always, wildcard-hiding, doom loop.** Plan slice: lines 4522–4720. Files: modify `internal/session/engine.go`, NEW `internal/session/policy.go`, NEW `internal/session/engine_perm_test.go`. Produced `func (e *Engine) RulesetFor(sessionID string) ([]protocol.Rule, error)` = builtins(agent, dataDir) + config permission rules + `db.AlwaysRules`. LOCKED gate order per tool call: (1) doom check (sliding 3-identical on per-turn `[]CallKey`; ask fires BEFORE the running part), (2) hidden guard (`tool not available`), (3) external_directory, (4) core Ask. Errors: deny → `permission rejected`, ctx-cancel-while-parked → `aborted`, doom deny metadata `{reason: doom_loop}`; model continues after gate failures. Per-round ruleset re-read drives tool hiding ("always" applies from the next round). Harness: `replyWatcher` + `queueReplies` + `setAgent` + `cfgLoader` Cfg seam. 5 tests (plan's 4 + a self-authored abort test — deviation 25). Gate green incl. `-race`. See deviations 22–25. Files: modify `internal/session/engine.go` (permission integration), NEW `internal/session/policy.go` (ruleset assembly + visible tools), NEW `internal/session/engine_perm_test.go`. Produces `func (e *Engine) RulesetFor(sessionID string) ([]protocol.Rule, error)` = builtins(agent, dataDir) + config permission rules + `db.AlwaysRules`. LOCKED per-tool-call semantics in order: (1) doom check — per-TURN history slice of `CallKey{tool, sha256(json args)}` (resets each Send); **sliding 3-identical window**: any call whose two immediate predecessors are identical triggers `Perm.Ask` action `doom_loop`, resource `[toolname]`; Deny → part error `permission rejected` (metadata reason `doom_loop`), model continues; a "once" reply does NOT extend the exemption (flagged to user, see resolutions #9); (2) external-directory check on `tool.External` paths outside the session dir → `Perm.Ask` `external_directory` `[dir/*]`; (3) core `Perm.Ask` with `Resources`/`Always` from `tool.Patterns`; Deny → part error `permission rejected`; abort/cancel during ask → part error `aborted` (service resolves pending as Deny on ctx cancel — verified T16 prep). "always" reply → service persists the rule (Task 10) → NEXT round's `RulesetFor` picks it up → identical calls skip the dialog; auto-covered pendings already arrive as Allow. Visible tools: `tool.Visible(RulesetFor, registry)` — hidden set per upstream `disabled()` (edit maps edit+write); hidden tools get no schema; if the model calls one anyway → part error `tool not available`. NOTE: T16's `executeTool` already implements the external-directory gate + core Ask inline — T17 extracts ruleset assembly into `policy.go` (`RulesetFor`, visible-tool helper, schema builder) and adds the doom check + hidden-call guard + `aborted` path; keep the passing T16 tests green. Plan test code is partially pseudocode — fix the obvious compile issues, keep the assertions (pattern: tests define contract). Harness additions (implement ONCE in `engine_test.go`, shared with T16 tests): `h.cfgPermission []protocol.Rule` (harness's `Cfg` closure reads it), `h.setAgent(t, ses, agent)` (db.UpdateSession + clear any engine prompt cache), `h.queueReplies(...)` = watcher goroutine on the bus for `permission.asked` → decode RequestID → consume queued replies FIFO → `h.permSvc.Reply(id, resp)`; unexpected prompt (queue empty >3 s) → `t.Fatal`; expose the permission service on the harness (`h.permSvc`) for always-rule assertions. Tests: `TestPermissionDenyStopsToolButNotTurn` (cfg deny rule `bash: cat *` → part error `permission rejected`, turn continues to the next scripted turn), `TestPermissionAlwaysPersistsAndSkipsNext` (read `.env` asks; reply `always`; 2nd identical call completes without a new ask; assert persisted always rule `{read, *, allow}` via `h.permSvc`/db), `TestHiddenToolNotSentToModel` (cfg `{edit: {"*": "deny"}}` → no edit/write in `req.Tools`, read/bash present), `TestDoomLoopThirdIdenticalAsks` (build agent, 4× identical `glob x*` → doom asks fire for the 3rd AND 4th calls (sliding window), queue two "once", exactly 2 `permission.asked` with permission `doom_loop`, all four tools eventually complete). Commit: `git add internal/session && git commit -m "feat: engine permission integration — ask/deny/always, wildcard tool hiding, sliding doom-loop"`.

**Task 16 (M4, DONE this session — `34c294e`): `internal/llm/fake` + session engine core.** Create `internal/llm/fake/fake.go` (scripted driver: `Turn{Parts, Err}`, `Driver{Turns, TitleTurns, ReqLog}`, `New(turns...)`, `Stream` emits parts in order, last MUST carry Finish; TitleTurns served when request system prompt starts with the title.txt marker), `internal/session/engine.go`, `engine_test.go`. Plan slice: lines 4206+ (read the whole slice before executing). Pinned core: `Deps{DB, Bus, Prov, Perm, Tools, DataDir, Cfg func(projectDir)(*protocol.Config,error), Drivers map[string]llm.Driver (test seam), Clock func() int64}`, `New(d Deps) *Engine`, `Send(ctx, sessionID, text, onDone) (SendResult{MessageID, PartID}, error)` (persists user msg async, `ErrSessionBusy`, per-session atomic busy flag), `Abort(sessionID) bool`, `Status(sessionID) protocol.SessionStatus`. History→LLM mapping (LOCKED, plan ~4262-4269): system msgs = BuildSystemPrompt entries as leading separate RoleSystem messages; user → RoleUser join(text parts,"\n") + PlanReminders appended to LAST user message content; assistant → RoleAssistant (text only — reasoning EXCLUDED) + ToolCalls from completed/error tool parts (`Args` = state input JSON); each tool part → one RoleTool msg after its assistant (completed→output, error→error text); empty assistant skipped. Tool schemas = `tool.SchemaFor` over `tool.Visible(permission.Flatten(ruleset), registry)`, ruleset = builtins(agent, dataDir) + config permission rules + session always-rules. Send finalize: assistant MessageRow (cost = usage×model cost per 1M; tokens), events `message.updated`/`message.part.updated`, status idle, `onDone(nil)`. **Config instructions[]**: the engine owns the `Cfg` dep and appends config instruction file contents after the BuildSystemPrompt output (AGENTS.md entry first — preserves the pinned order [family, env, AGENTS.md, config...]); Task 15's public `BuildSystemPrompt` deliberately does not load config. **Plan test typo**: harness struct field `.eng` (line 4297) is missing the `h` — fix when porting the test (pattern: tests define contract, compile-error typos get fixed, not worked around). Upstream refs re-clone pinned v1.18.18 into `/tmp/opencode-upstream` if wiped.

## Plan deviations logged so far (established pattern: tests define contract)

1. T2: plan test bugs (SessionWireShape model; MessageRoles key; id alphabet) — fixed in tests.
2. T3: `LoadAt` 4-arg → `LoadAt(globalDir, startDir)`; M5 global config file `global.jsonc` → `yolo.jsonc`.
3. T5: `sess.Model` is `*ModelRef` (compare `.ProviderID`/`.ID`); CallID not persisted in schema v1 (noted).
4. T6: `TestCancelStopsDelivery` needed `ok` check on closed-channel receive.
5. T7: `Part` gained `Args json.RawMessage`; test uses local `stream()` helper instead of plan's `.must`/`drainFinal` placeholders; midstream test drains from the same PartStream.
6. T8: plan's `common_test.go` was missing the `encoding/json` import — added.
7. T9: plan's fixture generator wrote the bare `opencode` entry, but both the test and parser expect the `{"opencode": ...}` wrapper — fixture regenerated with the wrapper (matches what the real fetch caches). `TestKidoParsesLlamacpp` passes `srv.URL+"/v1"` to keep its `/v1/models` path assertion meaningful. Zen auth test isolates via `XDG_DATA_HOME` + `OPENCODE_API_KEY=""`.
8. T10: two rule vocabularies — `protocol.Rule.Action` uses config/wire "allow"|"deny"|"ask" (new `RuleAllow/RuleDeny/RuleAsk` consts), while the `Decision` constants are "allow"|"denied"|"ask" (`Allow/Deny/AskAction`). Evaluations compare against the Rule consts. Auto-answered pendings (via `always` coverage) are stored `"once"` so only explicit `always` replies mint always rules. Plan's `service_test` had a dead `done <- Allow` goroutine before the real `Reply`; cleaned to a single `Reply` call.
9. T10: `Service` gained `SetConfigRules`/`SetDataDir` so `DecisionFor` can evaluate builtins + config rules + DB always rules without changing `New(db,bus)`; `DecisionPre==""||AskAction` triggers a fresh `decisionFor`; the catch-all `*`-permission rule only applies to known core actions on the decision path (unknown actions default to ask, matching upstream no-rule behavior).
10. T11: two plan test bugs — `TestReadByteCap` used 3000×9-byte lines (27000B total) which can never trip the 50KB cap under any accounting (changed to 3000×40-byte lines = 120000B so it cuts ~line 1305, before the 2000-line limit, as the test promises); `TestReadMissingFileSuggests` used siblings `app.go`/missing `ap.go` which never substring-match either direction under the pinned algorithm (changed to sibling `myapp.go`/missing `app.go`).
11. T11: test I authored (plan left it to the implementer) `TestTruncateSingleLineUTF8Cut` — verified against upstream `shell.ts tail()`: a single over-long line keeps its **last** MaxBytes bytes advanced to a UTF-8 boundary (40000×U+00E9 → keep 51200B = 25600 runes; 17100×U+65E5 mid-rune cut → 51198B = 17066 runes). Initial expectation of 14400 was the *removed* rune count, not kept.
12. T11 (environmental): on this host, a **plain** `import "embed"` + a scalar (`string`/`[]byte`) `//go:embed` fails typecheck with `embed imported and not used` under **both** installed toolchains (go1.26.5 via mise, and go1.25.10 via GOTOOLCHAIN) — an upstream-unused-import exemption is missing from the bundled types2 for scalar embed targets here (`embed.FS` targets work). Workaround used in `read.go`: `import _ "embed"` (still embeds content verbatim; runtime-verified on both toolchains). Flag: revisit if a future toolchain restores the plain-import path; the pinned sha256 pin in `TestDescPinned` guards against silent content drift regardless. `write.go`/`edit.go` reuse the same blank-import workaround.
13. T12: plan test bug — `TestEditPatternsAndExternal` expected `Patterns(raw)` to return the env.Dir-relative path (`sub/f.txt`), but the committed Task 11 interface (tool.go:72-73) takes raw args only: paths are emitted **as given**; the engine (Task 17) resolves/relativizes against Env.Dir (read does the same). Test fixed to assert as-given. Corollary design: edit `Patterns`/`External` parse only `filePath` (via `editFilePath`), matching the plan test's `{"filePath": f}`-only args and upstream (patterns built from the path alone); full old/new validation happens in `Run`.
14. T13: plan test bugs — (a) `TestGlobTool` expected 3 lines for `**/*.go`, but only 2 files can match (`.git/skip.go` is excluded by the hidden-skip rule the same test asserts; `a/z.md` is not `.go`) → fixed to 2. (b) `TestGrepTool` carried a dead `lines` variable (Go compile error: declared and not used) + a stray leading space in `joined :=` → dropped the dead loop, kept `joined`. Implementation note: `Patterns`/`External` for glob/grep emit the as-given `path` (or `["*"]` when omitted) per the raw-args-only interface; the engine resolves in Task 17.
15. T14 (implementation bug, found at Step 4): the plan pin (line 2739) is internally inconsistent — the marker `echo __YOLO_END_{n}_:$?_$(pwd | base64 -w0)` emits a line with a **colon** before `$?` (e.g. `__YOLO_END_0_:0_<b64>`), but the reader regex `^__YOLO_END_{n}_(\d+)_(\S*)$` has **no** colon, so it never matches → `Exec` hung until the 120s timeout (reader *did* receive the line; diagnosed via temp stderr logging + child-fd inspection). Resolved by keeping the plan's regex as authoritative and dropping the stray colon in the emitted marker → `echo __YOLO_END_{n}_$?_$(pwd | base64 -w0)`, so the emitted `__YOLO_END_0_0_<b64>` matches exactly. (Upstream v1.18.18 does NOT use this persistent-shell+marker design — it runs one-shot `shell -c` with `stdin:"ignore"`; the marker protocol is a plan-pinned v1 design, so we reconciled the plan's own two pins rather than upstream.)
16. T14 (implementation bug, found at Step 4): first stderr-merge used `cmd.Stderr = stdoutPipe.(*os.File)` where that file was the pipe's **read** end (O_RDONLY) — the child's writes to stderr failed silently (EBADF) and vanished, so `echo err >&2` produced no output (only the stdout marker arrived). Fixed by creating one output pipe via `os.Pipe()` and assigning the **write** end to both `cmd.Stdout` and `cmd.Stderr` (child dups it to fd 1 and 2), parent closes its write-end copy after `Start` so the reader sees EOF on child death; the read end is stored on `shellProc.stdout` and closed in `reapProc`.
17. T15: plan test fixes — (a) removed the `{"codex-mini","openai","codex.txt"}` family case (the plan itself, line 4111, notes it is wrong under the verbatim rule — no `gpt` substring → default.txt) and added `{"gpt-codex-1","openai","codex.txt"}` so rule 3's codex sub-branch stays covered; (b) the plan's `TestEnvBlock` `want` string is malformed (it embeds the author's annotation "yes|no — filled by BuildSystemPrompt; EnvBlock takes a bool" inside the expected prefix and can never match) → rewrote as an assertable prefix/suffix test; `EnvBlock` keeps the pinned 3-arg signature and runs the git detection itself (any failure → "no"), results cached per directory for the process lifetime (plan says "per dir + turn" — process-wide is the superset; a repo created mid-session is the only divergence).
18. T15: interpretation of the pinned instruction assembly — the test seam `buildSystemPromptForTest(dir, model, providerID, instructionPaths)` is the core (explicit instruction files, no config); the pinned public `BuildSystemPrompt(dir, model, apiID, providerID)` resolves the AGENTS.md walk-up + git detection but NOT `config.instructions[]` (its signature has no config; loading it would double-load what the engine already loads via the `Cfg` dep). The engine (Task 16) appends config instruction files after `BuildSystemPrompt` output — preserving the pinned order [family, env, AGENTS.md, config...]. `plan-mode.txt` is embedded + sha-pinned but has no Task-15 consumer (PlanReminders only uses plan.txt / build-switch.txt); expect the engine or a later task to consume it.
19. T16: plan interface contradiction — `Status(sessionID)` returns `string` (`protocol.StatusIdle`/`StatusBusy`) instead of the plan interface line's `protocol.SessionStatus` struct; the tests compare against the status strings, and the struct only appears inside the `session.status` event props (which the engine does emit). Followed the tests.
20. T16: tool part ID = model call ID (`llm.Part.CallID`; fresh `prt-` id fallback when the driver sends none). `CallID` is not persisted anywhere else in schema v1 (T5 note) and history replay must pair assistant `ToolCalls` with `RoleTool` results — making the part id the call id avoids a migration; `messagesFor` emits `ToolCallID: p.ID` accordingly.
21. T16: test/harness adaptations + engine behavior deltas — (a) plan harness discarded the registry (`_ = reg`) while `Deps` needs `Prov` → set `Prov: reg`; (b) `Clock` needs a `func() int64` closure; (c) the one-shot title side-call is fired from `Send` and races `ReqLog`, so the plan's `h.drv.ReqLog[0]`-based request assertions are racy → tests filter via `nonTitle(...)` (request whose first system message starts with "You are a title generator") and read requests through the new fake accessor `Requests()` (mutex-protected copy — direct `ReqLog` appends from concurrent `Stream` calls would race under `-race`); (d) fake driver: `Kind:"tool"` parts carry the args JSON in `Text` (LOCKED plan convention, documented on `fake.Turn`) plus a ctx-aware final error part on cancel; (e) round continuation: the engine continues when `finish=="tool_calls"` OR the round emitted any tool part (scripted drivers may carry `Finish:"stop"` on a tool round's last part — robustness rule that keeps the plan's replay test green, max 50 rounds); (f) first text/reasoning delta emits BOTH `message.part.updated` (create) and `message.part.delta`; later deltas delta-only; finalize emits the single final `part.updated` with End — satisfying "exactly one final part.updated"; (g) `onDone` fires exactly once from the turn goroutine's defer (after idle status + busy release).
22. T17: two of the plan's four pinned tests passed under T16 code ALREADY at Step 2 (always→skip: the service's `decisionFor` fresh re-evaluation already included `AlwaysRules` between `DecisionPre=AskAction` and parking; wildcard hiding: T16's inline `buildRequest` ruleset assembly already filtered schemas). Kept as regression guards; the differentiators (deny string, doom loop, abort mapping) failed at Step 2 and went green at Step 4.
23. T17: `Part.CallID` is not persisted (T5 note / deviation 20; part ID = call ID) — the perm test helpers key tool parts on `pt.ID` rather than `pt.CallID`.
24. T17: interpretations where the LOCKED semantics are silent — (a) hidden/unknown tool calls fail BEFORE the doom check and are NOT appended to the doom history (the history tracks calls that reach the permission gates); (b) a doom-denied call is saved directly as an "error" part (no "running" part ever exists — the doom ask fires before it by the plan's ordering pin) with `Time.Start = Time.End = now` and metadata `{reason: "doom_loop"}`; (c) `rulesetForRow` degrades a config LOAD ERROR to "no config rules" (non-fatal, matching T16's per-turn config tolerance) — `RulesetFor` errors only on session/DB/builtins failure; (d) the external_directory gate maps its own service errors to the same `permission rejected`/`aborted` strings as decision failures.
25. T17: self-authored fifth test `TestPermissionAbortDuringAskAbortsTool` — the plan's Step 1 lists four tests but pins the abort semantics ("Abort/cancel → part error `aborted`"); untested otherwise. Red at Step 2 (T16 mapped abort to `permission denied`), green at Step 4.
26. T18 (minor): the plan's 409-test seam "add `Delay` field to `fake.Driver`" was replaced with a ctx-aware sleep INSIDE the `slowDriver` wrapper (engine_test.go) — the title side-call and the turn call `Stream` concurrently, so a shared fake field would race under `-race`; `fake.Driver` is unchanged.
27. T18 (minor): `TestMaxToolStepsHalts` uses a UNIQUE glob pattern per call (`p0*`…`p50*`) — identical args would trip the T17 doom-loop gate (default `ask`) on the 3rd call, parking asks nobody answers (replyWatcher t.Fatal after 3s). The LOCKED assertion (exactly 50 persisted tool parts) is unchanged.
28. T18 (minor): the plan's tests stored the turn error in a bare `var doneErr error` and read it after `waitIdle` — a data race: `runTurn`'s defer releases busy BEFORE calling `onDone` (deviation 21g ordering). Tests now use the established `done chan struct{}` barrier and additionally assert `onDone(err)` for retry give-up and mid-stream failure (the plan left `_ = doneErr` unasserted; the LOCKED semantics pin `onDone(err)`).
29. T18 (info): `synthetic` round-trip added to `internal/storage/dao.go` (text/reasoning parts store `{"synthetic":true}`) — required for the "note excluded from history replay" contract once the DB re-read happens in `messagesFor`; not pinned by a test, covered by the overflow/midstream tests' observable paths. Also: retry-event counting waits for the turn's final idle `session.status` event before `eventCount` (bus delivery is channel-based and order-preserving); the plan's test ranged `h.events` directly (racy + delivery-lag).
30. T19 (M0 reconcile, important): the plan's "Files" lists only `internal/server/{server,handlers_session,sse,errors,server_test}.go`, but the pinned `Deps` shape forces: dropping M0 Task 1's `New(workDir, ...Opt)`/`Scope`/`Opt` API in place (`Server` keeps Start/Addr/Close listener surface; `NewServer` added); touching `internal/protocol/command.go` (NEW pinned DTO `CommandResponse`, M5 4978), `internal/storage/db.go` (export `ProjectID` wrapper), `internal/auth/auth.go` (`LoadFrom`/`SaveTo` + delegation), `internal/config/config.go` (`LoadGlobal`/`SaveGlobal` + map helpers), `internal/provider/seams.go` (`NewStaticForTest`), `internal/llm/fake/fake.go` (auto-turn + script seams), `cmd/yolo/main.go` (`NewServer` wiring, default port 4096 per M5). Commit stages all of those (plan staged `internal/server internal/llm internal/provider` only).
31. T19 (plan test bug, important): as written, `TestSendMessage409AndEvents` pre-reads one SSE event BEFORE the POST (nothing is ever published pre-subscribe → the test hangs forever; the read's `line` var is also unused → Go compile error). Fixed by moving frame reads to after the send; every assertion unchanged. The pinned "harness" block was pseudocode (`newSrv` with `…` placeholders, `clientWithHeader`) — filled with real `httptest` + `storage.Open` (parent dir MkdirAll first) + `permission.New` + `tool.Registry()` + zero-value `config.Loader` + real Clock + `Drivers{"kido": fake}`.
32. T19 (interpretation, important): M5 4972 pins `/project/current.id` = `prj_` + 20-char base62 of sha256(dir)[:10], but the M1 (Task 5, locked + tested) storage scheme is `prj_` + 24 hex chars of sha256. The TUI correlates session `projectID` (set by storage) with `/project/current.id`, so the server reuses the storage scheme via the new `storage.ProjectID` export. Pinned assertion (prefix + single `prj_` occurrence) passes under the hex scheme. Corollary: M5 4977 says "five functions" but names exactly four signatures (`auth.LoadFrom`, `auth.SaveTo`, `config.LoadGlobal`, `config.SaveGlobal`) — implemented exactly those four.
33. T19 (interpretation, minor): `POST /session/{id}/abort` blocks until `Engine.Status(id)=="idle"` (≤2s, 10ms poll) before returning 200 — the pinned `TestAbortEndpoint` reads `GET /session/status` immediately after the abort 200 and requires idle; without the settle-poll it would race the turn goroutine's defer (engine abort semantics unchanged).
34. T19 (self-authored tests, minor): plan pins 5 server tests; added `TestMessagesEndpoint` (202 send → poll status idle → GET message shows user text "hello" + assistant part `ok-*`), `TestScopedHeaderURLDecoded` (URL-encoded header with spaces resolves; malformed `%zz` → 400), `TestFakeFromEnv` (YOLO_LLM unset / `fake` without script / other value / script round-trip). All green first attempt; `-race` clean.
35. T19 (design, minor): fake grows `Turn.Auto` (sticky "auto-turn" flag consumed by `New`, synthesized on demand — "ok-<1-based request count>", Usage{1,1}, Finish stop), `AutoText()`, per-turn `Turn.Delay`, `SetDelay` (applies to synthesized turns only; scripted turns carry their own delay), `FromScript(path)` (pure scripted, `[{"parts":[...],"delay_ms":N}]`). This replaces the T18 deviation-26 `slowDriver` wrapper approach for server-level slow-turn tests (409/abort) — `SetDelay` is mutex-guarded; cancel during delay/emit → part `Finish:"error", Err: ctx.Err()`.
36. T19 (info): Go 1.22 `ServeMux` method+path patterns; per-method catch-alls (`/{tail...}`) return the 404 envelope — unknown-METHOD (405) stays stdlib text, since M5 pins "unknown route → 404 envelope" for paths; literal `GET /session/status` beats `GET /session/{id}` by stdlib specificity; `envelope`/`decode`/`scope` unexported per M5 4977; `Server.Close` grace = 2s (M0 ns-literal dropped); SSE subscribes after the 200 handshake flush; `handleSend` passes `context.Background()` (a turn must outlive its HTTP request; abort flows via `Engine.Abort` → turn-ctx cancel).
37. T20 (plan test bug, minor): the pinned `TestGlobalConfig` writes/reads `<Home>/yolo/global.jsonc`, but M5 4977 (LOCKED) + T3 deviation 2 target `<globalDir>/yolo.jsonc` — test line fixed to `yolo.jsonc` (deviation 2 stands; all LOCKED assertions unchanged).
38. T20 (interpretation, minor): M5's unscoped list omits `/provider`, but the pinned test sends `x-yolo-directory` to `GET /provider` to discover a config provider → the handler resolves the scope (workdir fallback), keeping config providers per-request-dir. `/provider/auth` stays unscoped (WorkDir).
39. T20 (info, minor): the pinned test's `writeCfg` uses `"base_url"`, but the locked `protocol.ProviderConfig` tag is `"baseURL"` — the handler therefore includes any config-defined provider that has models (`len(Models) > 0`) rather than requiring a base URL; the unknown `base_url` key is harmlessly ignored by the merge.
40. T20 (self-authored test + implementation surface, minor): added `TestProviderAuthEndpoint` (asserts the pinned `/provider/auth` keys for kido + opencode: `key_required`/`env`/status). Implementation surface: per-request auth recompute order env → auth.json → config `apiKey` (mirrors `auth.ResolveKey`); `!KeyRequired` → `not-required`; reply validates the body BEFORE the id lookup (LOCKED 400-before-404); NEW `permission.ErrNoPending` sentinel (wrapped by `Reply`); `provider.FromConfig` export; `NewStaticForTest` variadic zen seed.
41. T21 (plan pin contradicts engine + upstream, important): the plan's LOCKED SSE ordering pins `session.status busy` as the FIRST frame, followed by the user `message.updated`/`message.part.updated`. The engine (faithful to upstream v1.18.18 — `packages/opencode/src/session/prompt.ts` calls `session.createUserMessage`/part persistence ~:1057 BEFORE the busy `session.status` set ~:1089; yolo `engine.go` Send:163-164 publishes the user message+part synchronously, then the turn goroutine sets busy at runTurn:285) emits user events first. `TestSSEOrdering` asserts the ACTUAL faithful order (user msg → user part → busy → assistant round → part/deltas/final → idle); the engine was NOT changed. Plan pin error, tests define the contract.
42. T21 (plan miscount + placeholder interpretation, minor): the plan says "loop over the 8 scoped routes" for the scoping matrix, but exactly 7 routes resolve a session id via `scopedSession` (GET/PATCH/DELETE `/session/{id}`, GET/POST `/session/{id}/message`, POST `/session/{id}/abort`, POST `/session/{id}/command`) — all 7 asserted. Separately, the plan's timestamp placeholder `T1` is implemented as one stable `<T>` for every epoch-millisecond value (>= 1e11), while ids follow the plan's numbered scheme (`SES1`/`MSG1`/… deduped per concrete id) plus `<DIR>`/`<DIRNAME>` for the project dir; byte-stability (the actual LOCKED requirement) holds either way.
43. T22 (interface vs wire, minor): the plan pins `Client.Status(ctx) (map[string]protocol.SessionStatus, error)`, but the `GET /session/status` wire (T19 impl, T21 golden) is `{"sessions":{id:"idle"|…}}` — plain strings, which cannot decode into `map[string]protocol.SessionStatus`. `Status` returns `map[string]string` (documented on the method); the `SessionStatus` struct remains the SSE `session.status` property type (and `Store.Status`'s type, per the plan).
44. T22 (move mechanics, minor): the plan's Step 3 says T19–T21 tests "keep compiling unchanged" via an alias (`server.TestServer` → testutil). Impossible — the harness struct and its seam fields/methods are unexported and inaccessible from another package. Resolved with a full move: harness → `internal/server/testutil` in exported form (`TestServer`, `Boot`, `BootWithDriver`, `WaitSubscribe`, `FakeDelay`, `ParkAsk`, `WriteCfg`, `Req`, `SSEFrame`, `SSEReader`/`SSEConnect`/`Frame`) + mechanical reference renames in all three server test files. No behavior change; suite green incl. `-race -count=5`.
45. T22 (plan sketch test bug, minor): the sketch's `TestClientScopingAndErrors` asserts `x-yolo-directory == "/abs/dir"` (raw), but the wire carries the URL-escaped directory — upstream SDK sends `encodeURIComponent(directory)` (`server-compat.test.ts` expects `%2Frepo`), our server `PathUnescapes` (T19 `TestScopedHeaderURLDecoded` proves), so the client sends `url.PathEscape(Dir)` (slashes encoded) and the test asserts the escaped value.
46. T23 (dep path, minor): AGENTS.md/plan pin the dev-only teatest at `charm.land/x/exp/teatest/v2`, but the module's own `go.mod` declares `module github.com/charmbracelet/x/exp/teatest/v2` — `charm.land` does not serve it. Used `github.com/charmbracelet/x/exp/teatest/v2` at the SAME pinned pseudo-version `v2.0.0-20260816001655-68d539dca504` (identical content/version, path only). All other pins (`charm.land/bubbletea/v2` v2.0.8, `lipgloss/v2` v2.0.6, `bubbles/v2` v2.1.1) resolve from `charm.land` as pinned and are now `go.mod`-direct after `go mod tidy`.
47. T23 (plan sketch assumed bubbletea v1 API, minor): the T23 interface sketch is not buildable against the pinned bubbletea v2.0.8 as written: (a) `tea.Model` requires `View() tea.View` (a struct, not `string`) → implemented `View() tea.View { return tea.NewView(a.view()) }` with the pure `a.view() string` keeping all composition logic unit-testable; (b) `tea.Quit()` in v2 returns a `Msg`, not a `Cmd` → wrapped in `quitCmd()` for cmd returns; (c) the teatest sketch's `NewTestModel(…).Run()` is v1 — v2 flow is `NewTestModel(t, m, opts)` (auto-runs) + `Send`/`WaitFor`/`WaitFinished`/`Output`, and its output stream is CONSUMED by reads (`WaitFor` does an internal `io.ReadAll` per poll) — the locked assertions were kept as two `WaitFor`s (initial `Yolo`+`New session`, then `Hello · kido/q` post-hydrate) instead of a final-frame read; the resume-missing-session test's final `ReadAll` is safe because nothing consumed the stream first. Commit message and all locked render/behavior pins unchanged.

## Open items

- [ ] Execute Tasks 23–30 inline (per task commit messages in the plan)
- [ ] Flag at handoff: plan's Task 21 SSE pin (busy first) contradicts engine + upstream v1.18.18 (user message/part events precede busy; deviation 41) — verified against `/tmp/opencode-upstream` `prompt.ts`
- [ ] Task 30 tag `v0.1.0` ONLY with explicit user go-ahead (versioning: 0.1.0 = current scope; out-of-scope features → 0.2.0, …)
- [ ] On-demand live e2e vs `ai.kido.ws` (scripts/e2e-live.sh) — user-run, never CI
- [ ] Flags for user at handoff: plan-matrix third `edit` rule moved to engine (T10 note); CallID not persisted (T5); host toolchain scalar `//go:embed` typecheck gap → `import _ "embed"` workaround (T11 note 12)

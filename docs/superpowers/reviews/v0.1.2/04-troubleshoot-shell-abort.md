# golang-troubleshooting — shell-marker+abort
Date: 2026-08-20 · chunk: shell-marker+abort · source files: 8
## Findings
- [troubleshoot-1] P2 internal/session/engine.go:760-766 — max-tool-steps drop abandons the live `PartStream` while `turnCtx` is still alive: real drivers (`internal/llm/openai.go:170-174`, `anthropic.go:163-167` `send` = two-case select `ch <- p` / `ctx.Done`) block forever on the abandoned 64-slot channel (no ctx escape, turn exited `false, nil` so cancel never fires) → `defer body.Close()` never runs → a permanent goroutine + open TCP conn + unread response body leak per such turn on the long-running server; trigger is a single assistant stream with >~64 undelivered parts after the 50th tool part (i.e. ≥51 tool calls in one model message — the doom-loop scenario with "always" answers reaches this) — fix: derive a per-round context in `runRound` (child of `turnCtx`) and `defer cancel()` on every round exit so abandoned stream reads/sends unblock — contract-risk: none
- [troubleshoot-2] P2 internal/tool/bash.go:83-90 — `"timeout": 0` is accepted (`argInt` in read.go:109-115 admits 0; only `<0` rejected) → `timeoutMS = 0` → `Shell.Exec` skips the timer (shell.go:117 `timeoutMS > 0`) → the pinned 120 s per-command guard is silently disabled for that call, contradicting the error string "timeout must be a positive integer"; upstream v1.18.18 (`packages/opencode/src/tool/shell.ts:615-618,540`) treats 0 as valid but kills at `timeout + 100 ms`, so the port's `0 → unbounded` is also an upstream deviation — fix: reject `n <= 0` in `bashArgs` (or map 0 to the default) — contract-risk: behavior
- [troubleshoot-3] P3 internal/tool/shell.go:151-157 — the 10 MB cap slices `ev.raw[:room]` at a byte boundary; a cut inside a multi-byte UTF-8 rune leaves invalid UTF-8 in the model-visible output (wire-safe: json marshal maps it to U+FFFD); only bites on >10 MB output with multibyte runes exactly at the boundary — fix: back off to a rune boundary when the cap lands mid-rune — contract-risk: none
- [troubleshoot-4] P3 internal/tool/shell.go:107-111 — if the child dies between Exec start and the write, the EPIPE from `io.WriteString` is surfaced to the model verbatim as a raw OS error (e.g. "write |0: broken pipe") instead of a typed shell error; shell recycle itself is correct (`s.st = nil` + kill+reap) — fix: wrap in a stable `shell: command process died`-style error — contract-risk: none
- [troubleshoot-5] P3 internal/server/handlers_session.go:194-211 + internal/session/engine.go:201-211,305-314 — deleting a session with an ACTIVE turn does not cancel the turn: the FK cascade removes rows, the turn keeps publishing part/tool events for the deleted session until its next `CreateMessage` FK-fails, and a tool call after `Engine.Close` re-spawns a fresh bash process via `shellFor` that then sits in `e.shells` (of a deleted session) until process Shutdown (leaked process); narrow (delete-while-busy) — fix: call `Engine.Abort(id)` before/after `Close(id)` on delete — contract-risk: behavior (needs-upstream-check: upstream delete-while-prompt-active semantics not verified)
## Deferred / Noted (no fix in 0.1.2)
- scripts/e2e-live.sh:118 — `wait_text` abort check pipes `$b` through jq with `2>/dev/null`; an empty/invalid body (server died mid-poll) makes the `!= "0"` test return 2, misreporting "turn aborted before reply" instead of "server died" (live script only; P3, noted rather than finding)
- scripts/e2e-live.sh:129/156 — redundant nested `if [ -n "$SESS_ID" ]` under the same guard (cosmetic)
- engine.go:150-160 — `Send` error paths call `idleAndRelease` without invoking the stored `cancel()`; no leak (ctx parent is `context.Background()` with no waiters/children) — hygiene only
- Marker protocol probed, all edges REFUTED: child death mid-marker (bufio `ReadString` + `last` event path, shell.go:253-276), marker line split across pipe reads (bufio reassembles), CWD with special chars / long pwd (base64 -w0 + `([^\s]*)`), CRLF normalization, `set -e`/`set -x` state persistence (marker line itself still matches; `+`-prefixed xtrace never matches), stdin-consuming commands (`cat`) bounded by the 120 s timeout by design
- 409/abort settle-poll (deviation 33): Send's busy check+set is one `e.mu` critical section (engine.go:136-143) so Abort/Send cannot interleave; llm drivers use `http.NewRequestWithContext` (stream reads are cancel-interruptible); `perm.Ask` returns `(Deny, nil)` on ctx cancel → all three ask paths (doom/external/core) label the part "aborted" via the `ctx.Err()` branches; retry backoff select is ctx-aware (engine.go:631-636). "Abort during delay/emit" (deviation 35) is the fake driver's sticky-flag design — already cleared in 01-concurrency-llm-provider
- e2e env/global-variable audit (deviation 65 note): `req`'s documented global contract (`HTTP_STATUS`/`BODY`, never in command substitution) is honored at all call sites; `SERVER_PID` stays the yolo PID through the subshell+`exec env` chain; `trap cleanup EXIT` + `SERVER_PID=""` guard prevents double-wait; `$BASE_API` set before first `req`. Clean
- `Shell.onLine` is nil in all production callers (bash.go:105) — parameter is effectively test-only
- Evidence runs (all green, `-race -count=1`): `go vet ./...` clean; `go test ./internal/tool` (TestBash* incl. timeout-kill+respawn, stderr merge); `go test -run 'TestPermissionAbort|TestAbortMidTurn|TestShutdownAbortsActiveAndWaits' ./internal/session`; `go test -run 'TestAbortEndpoint|TestSendMessage409AndEvents' ./internal/server`
## Stats
P0:0 P1:0 P2:2 P3:3
COVERAGE: full — skipped: none (engine.go read target-adjacent in full: 1–389, 568–1172; 390–567 buildRequest/messagesFor are prompt assembly, not abort/cancel paths)

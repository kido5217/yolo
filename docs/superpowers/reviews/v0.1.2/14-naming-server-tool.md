# golang-naming — server-tool
Date: 2026-08-22 · chunk: server-tool · source files: 30
## Findings
- [naming-1] P3 internal/tool/write.go:119 — `diffCounts(old, new string)`: parameter `new` shadows the predeclared builtin `new` (skill "predeclared"/common-mistakes category); tooling and readers lose the builtin and diff sites read oddly — fix: rename the parameters to `before, after` (call sites: write.go:108, edit.go:155, diff_test.go:25) — contract-risk: none
- [naming-2] P3 internal/tool/diff_test.go:10 — table fields `old, new string` repeat the `new` builtin shadow, and the pair `added, rm int` mixes a spelled-out name with a truncated one for the same return pair — fix: `oldText, newText string` / `added, removed int` — contract-risk: none
- [naming-3] P3 internal/tool/shell.go:195 — `shellProc.stdout *os.File` holds the PARENT-side read end of the hand-rolled output pipe (the child's stdout is the write end `outW`, closed in spawnLocked); the name `stdout` misleads and a code comment must carry the semantics — fix: rename the field `outR` (or `readEnd`); single file, 4 sites — contract-risk: none
- [naming-4] P3 internal/tool/shell.go:206 — bool struct field `last` on `shellEvt` carries no `is` prefix (skill: bool fields MUST use `is`/`has`/`can` and read as questions) — fix: `isLast` (shell.go:138, :289; single file) — contract-risk: none
- [naming-5] P3 internal/tool/shell.go:56, internal/tool/shell.go:196 — opaque 2-letter abbreviations for live state: field `st` (the shell's `*shellProc`, 15 sites incl. `st2` in Exec) and `readr` (`*bufio.Reader`); a reader must consult the struct to know what `s.st` is — fix: field `proc *shellProc` (Exec locals `proc`/`proc2`) and `reader *bufio.Reader`; single file — contract-risk: none
- [naming-6] P3 internal/tool/tool.go:26 — unexported `Limits.def()` is a non-Go-sounding 3-letter name (reads as a Python keyword) for "fill zero fields with upstream defaults" — fix: `withDefaults()`; definition plus 4 call sites (bash.go:115, read.go:126, shell.go:66, truncate.go:10), single package, grep-verified — contract-risk: none
- [naming-7] P3 internal/tool/read.go:346 — `miss(fp) error` is a cryptic 4-letter name for "file not found, with up-to-3 sibling suggestions"; loop vars `lb`/`ln` (lowercase basename/name) are equally opaque — fix: `notFoundWithSuggestions` (or `suggestSimilar`) with `name`/`lowerName` locals; single file — contract-risk: none
- [naming-8] P3 internal/tool/read.go:368 — predicate `binaryFile(fp) bool` drops the `is` prefix (skill: boolean predicates read as questions, stdlib pattern `Is*`) — fix: `isBinaryFile`; single file — contract-risk: none
- [naming-9] P3 internal/server/errors.go:15 — `lob *log.Logger` is a non-standard 3-letter abbreviation used as the `recoverMiddleware` parameter, as the `bootLog` local (testutil.go:113), and even in the doc comment ("diagnosed to lob"); the codebase elsewhere spells the concept `Log`/`log.Logger` — fix: `logger` in both files — contract-risk: none
- [naming-10] P3 internal/server/handlers_misc.go:96 — `authState` returns two unnamed same-type strings (status, source), so call sites need positional memory (`st, _ :=`, `st, src :=`); skill: named returns for multiple same-type return values — fix: `(status, source string)`; single file, 3 call sites — contract-risk: none
- [naming-11] P3 internal/server/deps.go:6 — import alias `fakellm "…/llm/fake"` resolves no name collision in any file (the package name is `fake`); skill: alias only on collision — fix: drop the alias and use `fake`; 10 files across internal/server, internal/session, internal/tui — contract-risk: behavior
- [naming-12] P3 internal/server/server.go:64 — two constructors, `New` (returns `http.Handler`) and `NewServer` (returns `*Server`): a caller of `server.New(deps)` cannot see that a full *Server (auth state, listener lifecycle) is hidden underneath, and `New` names the return type ambiguously — fix: rename to `NewHandler` (def + the single caller testutil.go:118, grep-verified in chunk; production uses `NewServer`); alternatively keep the name and state the wrapping explicitly in the doc — contract-risk: none
## Deferred / Noted (no fix in 0.1.2)
- [naming-11] is auto-deferred (contract-risk: behavior): alias rename spans 10 files in 3 packages, most outside this chunk's scope.
- `lcsLen` interning map named `id` (internal/tool/write.go:130) reads as "identifier" but stores interned line codes; and locals `oa, na` are terse — borderline, left alone.
- `TestServer` fields `Eng`, `PermSvc` (internal/server/testutil/testutil.go:39,41) abbreviate while sibling fields spell out (`DB`, `Bus`, `Fake`, `Dir`) — inconsistent but test-only.
- `newSrvFakeEnv` (internal/server/contract_test.go:414) bakes the "Srv" abbreviation into a helper name — single-file, low value, noted only.
- `open` counter (internal/tool/todowrite.go:91) uses a bare adjective as a count — `openCount` would read better; cosmetic.
- `handleHealth`/`handlePath`/`handleProjectCurrent` live in handlers_session.go although handlers_misc.go exists for non-session/global routes — file-placement nit, no rename proposed.
- Working-directory concept is named `WorkDir` (server.Deps), `Dir` (tool.Env, testutil.TestServer), and lowercase `dir`/`d` everywhere; consistent enough per-layer, but a cross-package rename would be `behavior` if ever pursued.
- `editApply` params `contentOld, contentNew` (internal/tool/edit.go:166) — `oldContent, newContent` is the more idiomatic order; cosmetic.
- No violations found of: package/file naming, stuttering, receiver consistency, acronym casing (SSE/ID/LCS all correct), `New`/`NewTypeName` split outside [naming-12], error-string casing (all lowercase, no trailing punctuation), sentinel `Err` prefix, test-function names, or subtest casing (snake_case subtests comply with the skill's testing reference).
## Stats
P0:0 P1:0 P2:0 P3:12
COVERAGE: full — skipped: none

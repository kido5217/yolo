# golang-code-style — R15a
Date: 2026-08-22 · chunk: R15a · source files: 50 (non-test, scoped dirs: protocol, config, auth, bus, glob, log, storage, permission, llm, provider, session, server, tool, cmd/yolo) + 38 _test.go scanned (script-only, no flag-worthy hits)

## Findings
- [style-001] P2 internal/session/engine.go:594 — `runRound` is 227 lines with 11 parameters (`ctx, sessionID, agent, row, cfg, cfgRules, info, model, req, doomHist, toolCalls`) and unnamed `(bool, error)` returns; far beyond the skill's short/focused and ≤4-parameter guidance, and the parameter list duplicates the same session-context bundle on every sibling call — fix: extract a `roundCtx`/options struct (session id/agent, row, config+rules, provider info+model, req) and split the pre-stream retry loop from the stream pump — contract-risk: none
- [style-002] P2 internal/session/engine.go:920 — `executeTool` is 155 lines with 9 parameters, including the awkward `doomHist *[]permission.CallKey` pointer-to-slice and `asstID` threaded purely for persistence — fix: same options-struct extraction as style-001; make doom history round-local state instead of a pointer-in — contract-risk: none
- [style-003] P2 internal/session/engine.go:1077 — `saveToolPart` has 12 parameters; its own call sites (engine.go:1001, :1074) pass 12 positional args with two consecutive `""` fields, so argument identity is unreadable at the call — fix: take a `toolPart` struct (or build the Part inside) so call sites become named-field — contract-risk: none
- [style-004] P2 internal/session/engine.go:345 — recurring 5–8-parameter signatures on the same context bundle: `runTurn` (345, 6), `buildRequest` (417, 6), `messagesFor` (449, 6), `finishRound` (827, 8), `generateTitle` (1117, 5) — the skill's "fewer parameters, use an options struct" applies to the cluster, not just the worst function; a single `turn`/`round` state struct held on the engine per turn would collapse all of them — fix: group into one struct threaded through the turn — contract-risk: none
- [style-005] P3 internal/llm/anthropic.go:26 — `http.NewRequestWithContext` call with 4 arguments on one line (128 cols); same at internal/llm/openai.go:21 (136 cols) — skill: calls with 4+ arguments go one-arg-per-line — fix: break each argument onto its own line — contract-risk: none
- [style-006] P3 internal/tool/bash.go:109 — 239-col single error-message string; same pattern at internal/tool/edit.go:169 (177), :193 (143), :195 (126), internal/tool/read.go:318 (157), internal/tool/glob.go:154 (142) — fix: break long message strings with `+` concatenation at sentence boundaries (values unchanged) — contract-risk: none
- [style-007] P3 internal/tool/glob.go:40 — 294-col line: the "path" parameter description inside the glob tool's JSON schema literal; faithful upstream text — fix: if ever reflowed, MUST use `+` concatenation so the emitted schema string (sent to the model) stays byte-identical; same caution for the one-line composite literals at internal/server/handlers_misc.go:334, internal/provider/provider.go:253 (126), internal/server/testutil/testutil.go:131 (124) — contract-risk: pin
- [style-008] P3 internal/storage/dao.go:71 — one-line backtick SQL strings at 122–152 cols (also :93, :107, :171, :203, :401, :410, :491) and internal/storage/migrate.go:96 (125) — fix: wrap via string concatenation (`+`) rather than splitting inside backticks, which would inject newlines into the statement text — contract-risk: none
- [style-009] P3 internal/session/engine.go:706 — single statements over 120 cols: `e.publish(...)` at :706/:723/:873/:1094 (121–166) and the retry-exhaustion `fmt.Errorf` at :652 (128) — fix: break composite-literal args one-per-line; extract the errorf into a named boundary-error helper if convenient — contract-risk: none
- [style-010] P3 internal/tool/shell.go:146 — six-level nesting in the end-marker handling inside `Exec`'s read loop: `for`→`select`→`case`→`if marker`→`if b64 := m[2]`→`if p, derr := ...DecodeString` (→ `if dir := string(p)` at :151) — fix: extract a small `applyMarker(m []string) (code int, dir string)` helper to collapse the 3-deep decode chain — contract-risk: none
- [style-011] P3 internal/config/config.go:277 — `Merge`'s per-key path nests 5 levels deep on type assertions (`if ok` → `if dm, dok :=` → `if sm, sok :=` at :279–286) — the `continue`-inside-double-assertion shape obscures the four-way (map/map, instructions/slice, instructions/non-slice, leaf) decision — fix: one small `mergeValue(dst, src any) any` helper per case, or a leading `switch` on the value pair, flattens it to depth ≤2 — contract-risk: none
- [style-012] P3 internal/permission/service.go:76 — `EvaluateRules(agent, dataDir string, cfgRules []protocol.Rule, action string, resources []string)` has 5 parameters (126-col signature line); `dataDir` is process-constant and could live on the service (its doc at :58–59 already says so) — fix: store dataDir at construction and drop the parameter — contract-risk: none

## Deferred / Noted (no fix in 0.1.2)
- internal/server/handlers_misc.go:20,:22 (144/124 cols) — `baseAgents` / `commands` literals are LOCKED faithful text (upstream agent.ts, T20 command set); long lines are a consequence of the pinned strings — do not reflow (pin/wire)
- internal/tool/glob.go:40 is a pin-risk long line — kept as finding style-007 with `pin` tag, i.e. auto-deferred by the rule even though a value-preserving break is possible
- Naked returns (scan 2, ~130 hits): all verified inside void functions (handlers, `executeTool`, `saveToolPart`, `anReadSSE`/`oaReadSSE`, log methods) or 2–9-line closures with named returns (`editArgs`, `readArgs`, `bashArgs`, `writeArgs`, `globPathArg`, `finalizePart`); the arg-parsers' named-return + naked-return idiom matches the skill's remedy. No finding
- What-comments (scan 4, 492 hits): all are doc/contract comments (deviation references, LOCKED pins, upstream-parity notes, wire-shape docs); none restate adjacent code. No finding
- Commented-out code (scan 5): zero hits (single grep match is a doc-comment name reference in permission.go:79). None
- Positional composite literals: grep across scoped non-test files finds none; all struct literals use field names. None
- Deep-nesting sub-threshold/benign: internal/llm/anthropic.go:99–104 `anRequest` (5 levels, clear switch-case + 2-line guards), internal/llm/openai.go:251–262 accumulator ifs, internal/protocol/config.go:64 sort comparator, internal/tool/truncate.go:26 rune-boundary edge, internal/glob/glob.go:51 recursive matcher, internal/session/engine.go:530–535 per-part mapping (already switch+early-continue) — judged acceptable, no finding
- internal/tool/shell.go:94 `Exec` signature (138 cols) is long only due to named returns, has 4 params; not flagged
- Test files (38): long-line hits are wire-shape assertion strings (protocol/protocol_test.go up to 279 cols — pin/wire goldens) and SQL literals; naked returns only in short test helpers. Scanned, not read in depth; no findings
- internal/session/engine.go:1117 `generateTitle` (5 params, 56 lines) folded into style-004 rather than a separate finding

## Stats
P0:0 P1:0 P2:4 P3:8
COVERAGE: full — skipped: none (all 50 non-test scoped files scanned via the 4 prescribed scans + positional-literal/imports greps; hit regions read in ~15 files including engine.go, handlers_misc.go, both llm drivers, dao.go, shell.go, config.go; one-line signature extractions served as evidence for the rest)

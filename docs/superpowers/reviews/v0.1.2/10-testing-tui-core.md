# golang-testing — tui+core+cmd tests
Date: 2026-08-21 · chunk: tui+core+cmd tests · source files: 24
## Findings
- [testing-1] P2 internal/tui/permission_test.go:128-158 — TestPermissionKeyGate subtests ("1 replies once", "2 replies always", "3 replies reject") assert only `len(a.Cmds) == 1` on opaque `tea.Cmd` closures that are never executed, so the key→reply mapping (1→once, 2→always, 3→reject) is not pinned at the unit level; end-to-end, only '3' (reject render) is actually distinguished — the teatest "once"/"always" subtests (tui_suite_test.go:184-199) both proceed and render identically, so '1' or '2' wired to the wrong reply (e.g. '2'→"once", or '1'→"always") passes the entire suite, and the always-side observable effect (persisted rule / next identical ask auto-answered) is never observed after a TUI reply — fix: in the blackbox "always" run assert the persisted rule (second identical bash ask auto-answers with no dialog, or the rule is visible server-side) and in the "once" run assert no rule persisted; alternatively have the whitebox harness execute the recorded cmd (`cmd(t.Context())`) against a live test server and inspect the HTTP effect — contract-risk: behavior
- [testing-2] P2 internal/tui/model_test.go:199-220 — model/agent dialog subchoice keys 'a'/'b' are asserted count-only (same opaque-cmd class as testing-1; agent_test.go:107-126 + 176-194 identical): the 'b' = "set default" → config PATCH wiring is never executed end-to-end — the whitebox "default patch" subtests (model_test.go:272-293, agent_test.go:176-194) hand-inject a crafted `dlgPatchMsg` after keying 'b' instead of running the emitted cmd, and the blackbox runs (TestTUIModelDialog/TestTUIAgentDialog) only press 'a'; a 'b' cmd wired to the session-patch path or to the wrong field would pass everything — fix: extend one teatest run to press 'b' and assert the config default changed server-side (GET /config, or new session picks it up), or execute the recorded cmd against the live harness and assert the PATCH target — contract-risk: behavior
- [testing-3] P2 internal/bus/bus_test.go:53-72 — TestOverflowCancelsSubscribers under-verifies its named contract: the single `select` accepts either a buffered event (channel still open) or the close; in the event branch the test proceeds WITHOUT ever asserting the channel subsequently closes, and the only remaining check is that 10 further publishes don't panic — an implementation that drops events on overflow but never unregisters/closes the subscriber would still pass (verified: passes at 0.006s today) — fix: after exhausting the buffer, poll the channel with a bounded deadline and assert `ok == false` (subscriber dropped) as the primary outcome, keeping the no-panic check secondary — contract-risk: none
- [testing-4] P2 internal/permission/permission_test.go:13-31 — the multi-resource `Evaluate` reduction is pinned only for "any deny wins" (TestMultiResourceAnyDenyWins) and the no-rule→ask default; the upstream-pinned mixed case (upstream ask loop: per-pattern deny short-circuits, allow continues, anything else sets needsAsk → mixed ask+allow must ask) is untested — `Evaluate(rules, "read", ["a.env","a.go"])` under `[(*,*,allow),(read,*.env,ask)]` (the exact rule set from TestEvaluateFindLastNoMatchAsk) is never exercised, so a regression in the ask/allow reduction (turn parks vs proceeds) slips through at the unit level — fix: add the mixed ask/allow subcase asserting AskAction (and keep the deny-mixed case) — contract-risk: none
- [testing-5] P3 internal/tui/session_test.go:118-133 — TestRenderMessages table special-cases one case by its NAME string (`if tt.name == "empty store renders nothing"`) to swap the fixture; renaming the case silently restores the full fixture and the empty-render contract is silently unpinned — fix: replace with a struct field (e.g. `s store.Store` or `empty bool`) like the `mutate` field already in the table — contract-risk: none
- [testing-6] P3 internal/tui/footer_test.go:15-24 — footerApp's "The Current pointer is deep-copied" comment is wrong: `cp := *st.Current` is a shallow copy, so the `Model *protocol.ModelRef` pointer is shared across all subtests; today's mutations all reassign fields on the copy (safe), but any future mutation touching a Model field (e.g. `s.Current.Model.ID = …`) leaks into every other subtest — fix: copy the Model ref too or drop the "deep" claim — contract-risk: none
- [testing-7] P3 internal/tui/client/client_test.go:14-68 — one function bundles five independent scenarios (scoping-header escape, list decode, 409→ErrBusy, 404→ErrNotFound, 400→ErrBadRequest) with no subtests, so the first Fatalf loses the outcomes of later scenarios and `go test -run` can't target one; same shape in internal/tui/store/store_test.go:157-181 (three unrelated foreign-session scenarios in one test) — fix: split into named `t.Run` subtests per scenario — contract-risk: none
## Deferred / Noted (no fix in 0.1.2)
- testing-1 + testing-2 auto-deferred (contract-risk: behavior — fixes pin observable state: persisted permission rules / config patch target).
- Pinned gate runs `go test ./...` without `-race` or goleak; the concurrency-heavy packages in this chunk (bus, permission service round-trips, cmd drain/SIGTERM) are race-clean only under ad-hoc reviewer runs — a `-race` gate or TestMain goleak is a 0.2.0 process item.
- cmd/yolo subprocess tests (buildBinary `go build`, SIGTERM child, TestServeSigtermDrainsAndExitsZero) are integration-grade; the skill's `//go:build integration` split would change the pinned gate, so they stay in `go test ./...` by design.
- No `t.Parallel()` in any file of this chunk (skill SHOULD-level): justified by package-global mutation (toastTTL in toast_test.go) and shared teatest fixtures; noted, not chased.
## Stats
P0:0 P1:0 P2:4 P3:3
COVERAGE: full — skipped: none

## Status (wave 10 fix pass, 2026-08-21)
- testing-1 → DEFERRED contract-risk: behavior (auto-deferred by wave rule; fix pins persisted-rule observable state)
- testing-2 → DEFERRED contract-risk: behavior (auto-deferred by wave rule; fix pins config-patch target)
- testing-3 → FIXED 1beb970
- testing-4 → FIXED 1beb970
- testing-5 → FIXED d076676
- testing-6 → FIXED d076676
- testing-7 → FIXED d076676

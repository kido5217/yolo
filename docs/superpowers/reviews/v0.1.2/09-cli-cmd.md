# golang-cli — R9a
Date: 2026-08-21 · chunk: R9a · source files: 3
## Findings
- [cli-1] P2 cmd/yolo/main.go:276 — `tea.Run`'s error is only mapped to an exit code; `runErr` is never printed, so a TUI that fails to start (no TTY, terminal I/O error) exits 1 with zero diagnostic, unlike every other error path in `run` — fix: before `return tuiExit(runErr)`, print `fmt.Fprintf(os.Stderr, "yolo: %v\n", runErr)` when `runErr != nil && !errors.Is(runErr, tea.ErrProgramKilled)` — contract-risk: behavior
- [cli-2] P2 cmd/yolo/main.go:252 — the `stop` signal channel in `tuiMode` is registered but never read; the comment at main.go:249-251 claims "bubbletea's own handler ends the program", but bubbletea v2.0.8 (`tea.go:639-679`) maps SIGINT to `InterruptMsg` sent to the model (which no TUI file handles — grep confirms zero `InterruptMsg` matches in internal/tui), so a process-level `kill -INT <pid>` is permanently swallowed and only SIGTERM (QuitMsg) ends the TUI; SIGINT should interrupt — fix: capture the `*tea.Program`, and select on `stop` outside `Run` to call `prog.Interrupt()`/`Kill()` (or handle InterruptMsg in the TUI model to quit) — contract-risk: behavior
- [cli-3] P2 cmd/yolo/main.go:380 — `auth add` ignores `ReadString`'s error and never validates the key, so `yolo auth add kido </dev/null` (or `add kido ""`) silently persists an empty credential (Store.Set upserts unconditionally, internal/auth/auth.go:88); later turns then fail with a confusing auth error instead of failing fast — fix: reject an empty key after `TrimSpace` with exit 1 and a stderr message; treat `io.EOF` with non-empty line as valid — contract-risk: behavior
- [cli-4] P3 cmd/yolo/main.go:49 — `-v`/`--version` unsupported: the dispatcher falls through to `tuiMode`, which fails flag parsing with exit 2; upstream registers a version flag with alias `v` (packages/opencode/src/index.ts:51-52) and the flag alias is a universal CLI expectation — fix: add `case "-v", "--version":` to the `run` switch forwarding to the same version print — contract-risk: behavior
- [cli-5] P3 cmd/yolo/main.go:58 — version string `yolo 0.0.0-dev` is hardcoded; no ldflags injection point exists anywhere in the module, so Task 30's versioning (v0.1.0 tag) will need a build-time mechanism (the skill's `ldflags -X` pattern) — fix: add a `var version = "0.0.0-dev"` and document the ldflags build recipe when versioning lands — contract-risk: behavior
- [cli-6] P3 cmd/yolo/main.go:51 — `yolo help` / `-h` / `--help` prints usage to stderr; help text is program output and conventionally (and in upstream, yargs) goes to stdout, so `yolo help | less` and capture-based scripts see an empty stream — fix: `usage(os.Stdout)` for the explicit help paths, keep stderr for error contexts — contract-risk: behavior
- [cli-7] P3 cmd/yolo/main.go:304 — unexpected positional arguments are silently ignored: `yolo serve junk` serves fine and `yolo auth list extra` / `yolo auth add kido k1 k2` drop the extras; `tuiMode` validates `NArg() > 1` but `serve` (fs.NArg ignored) and `authCmd` (rest[len] ignored) do not — fix: after flag parsing in `serve`, and after the subcommand in `authCmd`, reject `len(remaining) != 0` with the usage and exit 2 — contract-risk: behavior
- [cli-8] P2 cmd/yolo/main_test.go:1 — `authCmd` (~70 lines, secret-handling surface: subcommand dispatch, missing-arg usage, empty-key/EOF prompt, load/save errors) has zero tests in cmd/yolo; the dispatch exit codes (unknown flag → 2, too many args → 2, `version` → 0) are also untested — fix: table-driven tests driving `authCmd` against `auth.LoadFrom`/`SaveTo` with `t.Setenv("XDG_DATA_HOME", ...)` and scripted stdin, plus a few `run`-dispatch cases via `exec.Command` — contract-risk: none — status: FIXED b693502
- [cli-9] P3 scripts/e2e-live.sh:57 — only `trap cleanup EXIT` is set; bash skips the EXIT trap on an untrapped SIGINT/SIGTERM, so `kill <script-pid>` (or any single-PID signal) leaves the backgrounded `yolo serve` running and billing turns (terminal Ctrl-C is safe only because the background job shares the script's process group) — fix: add `trap 'exit 1' INT TERM` (or `trap cleanup INT TERM`) before the EXIT trap — contract-risk: behavior
- [cli-10] P3 scripts/e2e-live.sh:153 — when step 3 times out (rc=1, "no assistant text reply"), the server log tail is only shown for the aborted case (rc=2), so a server that died 2 s into the turn is reported as merely slow; the two failure modes are indistinguishable from the output — fix: also `tail -5 "$SERVER_LOG"` in the timeout branch (or before the `case`) — contract-risk: behavior
- [cli-11] P3 cmd/yolo/main_test.go:25 — `TestDispatchServeFlag` tests only `yolo help` output (presence of subcommand names); the name implies serve-flag dispatch which is not asserted, misleading for future readers — fix: rename to `TestHelpListsSubcommands` or extend it to cover a serve flag error path — contract-risk: none — status: FIXED 2a3e936
## Deferred / Noted (no fix in 0.1.2)
- Upstream `serve` network options (`--port`/`--hostname` split, `server.port/hostname` config fallback, mdns, cors — packages/opencode/src/cli/network.ts) are not ported; `--addr` is the port's own CLI simplification and no `server.*` config surface exists (deliberate scope cut, not a dead surface)
- scripts/e2e-live.sh: the `/tmp/yolo-e2e.*` scratch dir (DB, config, server.log) is never cleaned up by the script
- `yolo serve`: a second SIGTERM during the 5 s drain lands in the full signal buffer and is dropped — no force-kill escalation (drain is bounded by the 5 s ctx; process exits regardless)
- `tuiMode`: a SIGINT arriving before `tea.Run` starts (terminal in cooked mode, e.g. during the 5 s session-existence check) is swallowed by the never-read `stop` channel — same fix as cli-2
## Stats
P0:0 P1:0 P2:4 P3:7
COVERAGE: full — skipped: none

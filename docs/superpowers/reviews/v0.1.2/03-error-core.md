# golang-error-handling — core+storage
Date: 2026-08-20 · chunk: core+storage · source files: 25
## Findings
- [error-1] P2 config/config.go:203 — `readFile` returns the raw `UnmarshalJSONC` parse error with no file identity; `LoadAt` (and `LoadGlobal`) merge up to 3+2·N+2 candidate files (global ×3, project chain ×2 each, `.yolo` ×2), so a malformed `yolo.jsonc` surfaces to the user as a bare `invalid character 'b' looking for beginning of value` with no way to tell which file is broken — skill rule 2 (wrap with context) — fix: in `readFile`, wrap as `return nil, fmt.Errorf("parsing %s: %w", path, err)` before returning `UnmarshalJSONC`'s error — contract-risk: none — STATUS: FIXED cbe91b8
- [error-2] P2 storage/dao.go:308 — `ProtocolToPart` discards `json.Marshal` errors (`b, _ := json.Marshal(p.State)` and the text-part map at :318); on failure `StateJSON` is silently persisted as empty, so the part's state is lost at write time and `PartToProtocol` later fails with a contextless `unexpected end of JSON input` instead of the write failing — skill rule 1 (never discard with `_`) — fix: make `ProtocolToPart` return `(PartRow, error)` and propagate; same fix covers :318 — auto-deferred — contract-risk: behavior (if this path ever fires, persisted part bytes change) — STATUS: DEFERRED contract (behavior — persisted part bytes would change on the marshal-failure path)
- [error-3] P3 storage/dao.go:149 — `if n, _ := res.RowsAffected(); n == 0 { return ErrNotFound }` discards the `RowsAffected` error (same at :186 `UpdateMessage`, :428 `ReplyPermission`); a driver-level failure in `RowsAffected` would be masked as `ErrNotFound`, misdirecting the caller — fix: `if n, err := res.RowsAffected(); err != nil { return err }` first, then the zero-check — auto-deferred — contract-risk: behavior (error identity changes in the failure corner) — STATUS: DEFERRED contract (behavior — error identity changes in the RowsAffected-failure corner)
- [error-4] P3 config/config.go:21 — `h, _ := os.UserHomeDir()` swallows the error (same at :29 `Data`, :37 `Cache`); when it fails (HOME unset and no passwd entry, e.g. some container contexts) the XDG roots silently degrade to relative paths (".config", ".local/share", ".cache") and yolo's config/data/auth files are placed under CWD instead of an absolute home — skill rule 1 — fix: propagate `(string, error)` from `Home`/`Data`/`Cache` (and update the four callers) so a broken home is a startup error rather than a silent misplacement — contract-risk: none — STATUS: FIXED cbe91b8
- [error-5] P3 glob/glob.go:120 — `mustCompile` explicitly discards the error (`_ = err`) while `segmentRe` carries an error return that is provably always nil: the documented fallback compiles `regexp.QuoteMeta(seg)`, which cannot fail, so the API advertises a failure mode that cannot occur and misleads readers about the code's invariants — fix: drop the error return from `segmentRe` (or adopt the skill's `Must*` panic idiom) and delete `mustCompile` — contract-risk: none — STATUS: FIXED 49299c8
- [error-6] P3 storage/protocol/auth test files — discarded setup/intermediate errors in tests: storage_test.go:59,60,61,76,77,78,96,97,117,128,129,130,146,147,148 (`_ = db.CreateSession/CreateMessage/UpsertPart/SavePermission/GetPart`), protocol_test.go:61,77,87,93,114,163,167 (`b, _ := json.Marshal`), auth_test.go:99 (`got, _ = auth.Load()`); when any of these fails, the test dies at a later, misleading assertion ("cascade failed", "tool state lost", "delete did not persist") instead of the root-cause error — skill rule 1 — fix: `t.Fatal(err)` / `t.Fatalf` on each setup call — contract-risk: none — STATUS: FIXED a7e98bd (also covers storage_test.go GetPart/PartToProtocol pair at :117-118, same discard class)
- [error-7] P3 storage/dao.go:336 — uncontexted error returns in decode paths: `PartToProtocol` (`:336` tool state, `:346` text state) and `ListMessages` token unmarshal (`:220`) and `AlwaysRules` always_json unmarshal (`:493`) return bare `json.Unmarshal` errors with no row identity, so one corrupt row fails an entire LIST with `invalid character ...` and no part/message/request ID; minor cousin at protocol/event.go:29 where `MakeEvent` returns the raw marshal error without the event type — skill rule 2 — fix: wrap with identity, e.g. `fmt.Errorf("part %s state: %w", r.ID, err)` (and `event %s: %w` in `MakeEvent`) — contract-risk: none — STATUS: FIXED a7e98bd
## Deferred / Noted (no fix in 0.1.2)
- auth/auth.go:105 `ResolveKey` swallows `Load()` errors (`if s, err := Load(); err == nil`) — verified FAITHFUL: upstream `Auth.all` (packages/opencode/src/auth/index.ts:65) uses `readJson(file).pipe(Effect.orElseSucceed(() => ({})))`, i.e. the same silent fallback; keep parity
- protocol/id.go:21 `panic(err)` on crypto/rand failure — defensible "corrupt invariant" panic (skill: panic allowed for truly unrecoverable states; CSPRNG death means the process cannot mint safe IDs); upstream equivalent `crypto.randomUUID()` throws unhandled
- log/log.go — all ten `_ =` discards are the documented best-effort design (package doc: "failures never propagate; no-ops until a later write retries"); not a finding. Related defer: logger is `fmt`-style Infof/Errorf, not `slog` (skill SHOULD); adopting slog would be a redesign of the pinned line format (`2026-08-17...Z <level> <text>`, log_test pinned) — out of 0.1.2 scope
- storage_test.go:19 `t.Cleanup(func() { db.Close() })` discards the Close error — idiomatic test-cleanup pattern, no action
## Stats
P0:0 P1:0 P2:2 P3:5
COVERAGE: full — skipped: none (all 25 scope files covered: 17 grep-hit files read in full; protocol/agent.go, protocol/command.go, protocol/provider.go, protocol/session.go, bus/bus.go, bus/bus_test.go verified pattern-free by grep (bus has zero error/Err/panic occurrences — fire-and-forget pub/sub); auth/auth_test.go and glob/glob_test.go read in full)

# golang-naming — llm-cmd
Date: 2026-08-22 · chunk: llm-cmd · source files: 17 (internal/llm 8, internal/llm/fake 2, internal/provider 5, cmd/yolo 2)
## Findings
- [naming-1] P3 internal/llm/openai.go:59,67 (also internal/provider/zen.go:157) — error strings not fully lowercase including acronyms (skill types-errors: "error strings MUST be fully lowercase — including acronyms"): `"upstream error (HTTP %d): %s"` ×2 and `"zen catalog: HTTP %d"` — fix: lowercase the acronym in the literals (`"upstream error (http %d): %s"`, `"zen catalog: http %d"`); verified by grep that no test, golden, or fixture pins these strings — contract-risk: none — status: FIXED ad393ec (re-verified: no pin in testdata/tests)
- [naming-2] P3 internal/llm/openai.go:42 — `oaUpstreamError` carries the OpenAI `oa` driver prefix but is driver-shared: internal/llm/anthropic.go:39 calls it for the Anthropic non-2xx path (comment: "same envelope handling"); the prefix misleads readers into thinking it is openai-only, and a future anthropic-specific change looks like it belongs to openai — fix: rename to `upstreamError` (3 sites: openai.go:33, :42, anthropic.go:39; same package, grep-verified) — contract-risk: none — status: FIXED ad393ec
- [naming-3] P3 internal/llm/fake/fake.go:106 — boolean local `synth` is an unexpanded abbreviation that does not read as a true/false question (skill: booleans MUST read as questions; is/has/can) — fix: `synthesized` (def + uses at fake.go:106-117) — contract-risk: none — status: FIXED ad393ec
- [naming-4] P3 internal/llm/fake/fake_test.go:266 — `texts := make([]llm.Part, n)` holds `llm.Part` values (one per goroutine; `texts[i] = p`), not text; the name misdescribes the value it represents — fix: rename to `parts` (fake_test.go:266-301; no other `parts` in that function) — contract-risk: none — status: FIXED ad393ec
- [naming-5] P3 internal/llm/anthropic_test.go:93 (also internal/llm/llm_test.go:139,184) — a `context.Context` local is named `drain`, the verb for the operation it bounds rather than the value it holds (skill identifiers table: `ctx` is the context.Context convention); call sites like `s.Next(drain)` read as if `drain` were an action — fix: rename to `drainCtx` at the 3 sites — contract-risk: none — status: FIXED ad393ec
- [naming-6] P3 internal/provider/zen.go:80 — unexported func `zenMeta` differs from exported type `ZenMeta` (zen.go:74) only by case; `meta := zenMeta(raw)` (provider.go:136) forces readers to disambiguate function from type by capitalization alone — fix: rename func to `parseZenMeta` (def zen.go:80 + the single call site provider.go:136; same package) — contract-risk: none — status: FIXED ad393ec
- [naming-7] P3 internal/provider/provider.go:195 — `cfgModel(mid string, mv any)`: parameter `mv` is a cryptic 2-letter abbreviation for the maximally ambiguous `any` argument (the model's raw config value); skill: parameters double as documentation at the call site, longer name when the type is ambiguous — fix: rename to `raw` (or `rawModel`) — contract-risk: none — status: FIXED ad393ec
- [naming-8] P3 internal/provider/provider.go:75 — boolean parameter `fill` on `OverridableDirs` is a bare verb, and both call sites pass a bare `true` (provider.go:114, provider_test.go:191) so the parameter name is the only documentation of the argument's meaning — fix: rename to `fillDefaults` — contract-risk: none — status: FIXED ad393ec
- [naming-9] P3 internal/provider/provider.go:311 — `Default()` returns two unnamed same-type `string`s (provider id, model id); skill: named returns when multiple return values share a type (same pattern as wave-14 server-tool naming-10); the single call site (`pid, mid := s.Prov.Default()`, internal/server/handlers_session.go:103) already supplies the names positionally — fix: `(providerID, modelID string)`; return names carry no reflection — contract-risk: none — status: FIXED ad393ec
- [naming-10] P3 cmd/yolo/main.go:47-61 (handlers at :212, :299, :341) — the three subcommand dispatchers mix three conventions for one concept (skill: inconsistent concept names): `serve` (bare verb), `authCmd` (Cmd suffix — it exists because renaming to `auth` would shadow the imported `internal/auth` package used inside it), `tuiMode` (noun for a run-function; skill: action functions are verbs — siblings `run`/`drain` are); a reader must track why each differs — fix: align on `serveCmd`/`authCmd`/`tuiCmd` (main.go:47,54,56,61,212,299,341 + main_test.go:316; main package only, grep-verified) — contract-risk: none — status: FIXED 26dfce8 (serve->serveCmd, tuiMode->tuiCmd; authCmd unchanged)
- [naming-11] P3 cmd/yolo/main.go:119 — `lob` is an opaque 3-letter abbreviation for the `*log.Logger` local used across all of `buildDeps` (main.go:119-190, 5 sites incl. the `fail`/`closeDB` closures); the codebase convention is `log.*`/`logger` (the identical abbreviation was independently flagged in the server chunk, wave 14) — fix: rename to `logger` — contract-risk: none — status: FIXED 26dfce8
- [naming-12] P3 cmd/yolo/main_test.go:285 — local helper `getStore` uses the Get prefix (skill: getters omit Get) for a disk-load action, and main.go:347 already names the same concept `loadStore` — fix: rename to `readStore` — contract-risk: none — status: FIXED 26dfce8
## Deferred / Noted (no fix in 0.1.2)
- `oaStreamOptions.IncludeUsage` (openai.go:107): bool wire-mapping field without an is/has/can form, mirroring the OpenAI API `include_usage` (explicit json tag, so a Go rename has zero wire impact); no natural predicate form available — considered, not flagged.
- `want*` table fields (cmd/yolo/main_test.go:330-334,457-461): consistent codebase-wide variant of the skill's `expected*`/`got` idiom; `want` is canonical Go test terminology — systemic, left to an explicit cross-chunk call (core chunk flag-8 covered only a repurposed `want` argument).
- `benchAnStream`/`BenchmarkAnReadSSE`/`benchOAStream`/`BenchmarkOAReadSSE`: the `An`/`OA` fragments mirror the production `an`/`oa` driver prefixes (case differs only with export position); unambiguous in context — not flagged.
- `provider.New` vs `NewWithSeams`/`NewStaticForTest`/`NewCatalogPolicy`: `New` returns the package's primary type `*Registry` and the other constructors stay disambiguated by signature — acceptable per the single-primary-type rule, not flagged.
- `import tea "charm.land/bubbletea/v2"` (main.go:22, main_test.go:18): self-alias, a no-op (the package name is already `tea`); costs zero and documents path≠package — noted only.
- `loadStore := auth.Load` (main.go:347): unnecessary local alias around `auth.Load` — cosmetic.
- `finished bool` + `finish` closure pair (anthropic.go:175-182, openai.go:202-210): standard Go idiom in this codebase; the name reads as a question — not flagged.
- `noNet` (kido.go:26): negated boolean parameter, descriptive and an accepted Go pattern — not flagged.
- `timeoutMS int` (kido.go:26): acronym rule-compliant (all-caps MS); preferring `time.Duration` is a design observation for the design chunk.
- fake `Driver.ReqLog` field vs `Requests()` method: two names for "recorded requests" (raw log vs detached-copy accessor); roles differ and are documented — not flagged.
- `TestErrTurn` (fake_test.go:137): terse but unambiguous in context — not flagged.
## Stats
P0:0 P1:0 P2:0 P3:12
COVERAGE: full — skipped: none
